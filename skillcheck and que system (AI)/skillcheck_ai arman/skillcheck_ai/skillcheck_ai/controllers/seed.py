from flask import Blueprint
from models import db
from models.question import Question

seed_bp = Blueprint("seed", __name__)

QUESTIONS = {
    "Python Developer": [
        ("What are Python decorators and how do you use them?",
         "A decorator is a function that takes another function as input and extends its behavior without modifying it. Decorators use the @symbol syntax. They are used for logging, authentication, caching, etc. Example: @login_required wraps a view function to check if user is logged in before executing it. They use functools.wraps to preserve the original function metadata."),
        ("Explain the difference between list, tuple, and set.",
         "List is mutable, ordered, allows duplicates - defined with []. Tuple is immutable, ordered, allows duplicates - defined with (). Set is mutable, unordered, no duplicates - defined with {}. Lists are used for collections that change, tuples for fixed data like coordinates, sets for membership testing and removing duplicates."),
        ("What is GIL in Python?",
         "GIL stands for Global Interpreter Lock. It is a mutex that protects access to Python objects, preventing multiple native threads from executing Python bytecodes simultaneously. This means only one thread runs Python code at a time even on multi-core systems. It simplifies memory management but limits CPU-bound multithreading. Use multiprocessing module for true parallelism."),
        ("How does memory management work in Python?",
         "Python uses automatic memory management with reference counting as the primary mechanism. Every object has a reference count. When it reaches zero, memory is freed. Python also has a cyclic garbage collector to handle circular references. The memory allocator uses private heaps. The gc module provides control over garbage collection. Memory is managed by the Python Memory Manager."),
        ("What are generators and iterators?",
         "An iterator is any object with __iter__ and __next__ methods. A generator is a simpler way to create iterators using yield keyword. Generators are lazy - they produce values one at a time and don't store all values in memory. This makes them memory efficient for large datasets. Example: def count_up(n): for i in range(n): yield i. Generator expressions use () instead of []."),
        ("Explain OOP concepts in Python.",
         "Python supports all four OOP pillars. Encapsulation: bundling data and methods, using _ and __ for private attributes. Inheritance: child class inherits parent class using class Child(Parent). Polymorphism: same method name different behavior in different classes. Abstraction: hiding implementation using abstract classes with ABC module. Python supports multiple inheritance unlike Java."),
        ("What is the difference between deepcopy and shallow copy?",
         "Shallow copy creates a new object but references the same nested objects. Changes to nested objects affect both copies. deepcopy creates a completely independent copy of the object and all nested objects. Use copy.copy() for shallow and copy.deepcopy() for deep copy. For simple objects with no nesting, both behave the same. For complex nested structures, use deepcopy to avoid unintended side effects."),
        ("How do you handle exceptions in Python?",
         "Python uses try-except-else-finally blocks. try contains code that might raise exception. except catches specific exceptions like except ValueError. else runs if no exception occurred. finally always runs for cleanup. You can raise exceptions with raise keyword. Custom exceptions inherit from Exception class. Use multiple except blocks for different exception types. Always catch specific exceptions rather than bare except."),
        ("What are lambda functions?",
         "Lambda functions are anonymous single-expression functions defined with lambda keyword. Syntax: lambda arguments: expression. They are used for short throwaway functions, especially with map(), filter(), sorted(). Example: square = lambda x: x**2. Limitations: only one expression, no statements, no docstrings. For complex logic, use regular def functions. They are commonly used in functional programming patterns."),
        ("Explain list comprehension with an example.",
         "List comprehension is a concise way to create lists. Syntax: [expression for item in iterable if condition]. Example: squares = [x**2 for x in range(10) if x % 2 == 0] creates list of squares of even numbers. It is more readable and faster than equivalent for loops. Can also do nested comprehensions. Dictionary and set comprehensions use {} instead of []."),
        ("What is the difference between __str__ and __repr__?",
         "__repr__ is for developers - it should return an unambiguous string representation that ideally can recreate the object. __str__ is for end users - it returns a readable human-friendly string. repr() calls __repr__, str() calls __str__. If __str__ is not defined, Python falls back to __repr__. In interactive console, __repr__ is used. Best practice: always define __repr__, optionally define __str__ for user-facing display."),
        ("How do you optimize Python code for performance?",
         "Use built-in functions and libraries which are implemented in C. Use list comprehensions instead of loops. Use generators for large data. Profile code first with cProfile or line_profiler before optimizing. Use NumPy for numerical operations. Avoid global variables. Use local variables in loops. Use slots in classes to reduce memory. Consider using PyPy for compute-heavy code. Cache expensive computations with functools.lru_cache."),
    ],
    "Java Developer": [
        ("What is the difference between JDK, JRE, and JVM?",
         "JVM (Java Virtual Machine) executes Java bytecode and provides platform independence. It manages memory, garbage collection, and security. JRE (Java Runtime Environment) includes JVM plus standard libraries needed to run Java applications. JDK (Java Development Kit) includes JRE plus development tools like compiler javac, debugger, javadoc. To develop: use JDK. To run: JRE is sufficient. JVM is the core runtime engine."),
        ("Explain OOP principles in Java.",
         "Java supports four OOP principles. Encapsulation: binding data and methods using classes, access modifiers private/public/protected control visibility. Inheritance: class extends another class using extends keyword, promotes code reuse. Polymorphism: method overloading (compile time) and overriding (runtime). Abstraction: hiding complexity using abstract classes and interfaces. Java does not support multiple inheritance through classes but does through interfaces."),
        ("What is the difference between abstract class and interface?",
         "Abstract class can have abstract and concrete methods, constructors, instance variables. A class can extend only one abstract class. Interface before Java 8 had only abstract methods. From Java 8 onwards, interfaces can have default and static methods. A class can implement multiple interfaces. Use abstract class when classes share common state and behavior. Use interface to define a contract or capability that unrelated classes can implement."),
        ("How does garbage collection work in Java?",
         "Java's garbage collector automatically manages heap memory. It uses generational collection - Young Generation (Eden, Survivor spaces) for new objects, Old Generation for long-lived objects. Minor GC collects Young Generation frequently. Major/Full GC collects entire heap. GC algorithms include Serial, Parallel, G1, ZGC. Objects unreachable from root references are collected. You cannot force GC but can suggest with System.gc(). Tuning GC is critical for performance."),
        ("What are Java Collections?",
         "Java Collections Framework provides data structures and algorithms. Key interfaces: List (ArrayList, LinkedList) - ordered, allows duplicates. Set (HashSet, TreeSet, LinkedHashSet) - no duplicates. Map (HashMap, TreeMap, LinkedHashMap) - key-value pairs. Queue (LinkedList, PriorityQueue) - FIFO. Stack for LIFO. Collections utility class provides sort, shuffle, binarySearch. Use generics for type safety. Choose based on use case: ArrayList for random access, LinkedList for frequent insertions."),
        ("Explain multithreading in Java.",
         "Multithreading allows concurrent execution of two or more threads. Create threads by extending Thread class or implementing Runnable interface. Runnable is preferred for flexibility. Use synchronized keyword for thread safety. wait(), notify(), notifyAll() for inter-thread communication. Java 5 introduced ExecutorService, Future, Callable for better thread management. java.util.concurrent package has thread-safe collections like ConcurrentHashMap. Avoid deadlocks by consistent lock ordering."),
        ("What is the difference between String, StringBuilder, and StringBuffer?",
         "String is immutable - every modification creates a new object. Inefficient for repeated string manipulation. StringBuilder is mutable and not thread-safe but faster. Use in single-threaded environments for string manipulation. StringBuffer is mutable and thread-safe (synchronized methods) but slower than StringBuilder. Use StringBuffer in multi-threaded scenarios. For concatenation in loops, always use StringBuilder. String pool optimization applies only to String literals."),
        ("What are Java generics?",
         "Generics allow writing type-safe code that works with different types. They provide compile-time type checking and eliminate casting. Syntax: class Box<T> where T is type parameter. Bounded types: <T extends Number> limits to Number subclasses. Wildcards: <?> unbounded, <? extends T> upper bounded, <? super T> lower bounded. Type erasure removes generic type info at runtime. Generics improve code reusability and catch type errors at compile time rather than runtime."),
        ("Explain exception handling in Java.",
         "Java has checked exceptions (must be handled or declared) and unchecked exceptions (RuntimeException subclasses). try-catch-finally structure handles exceptions. catch can handle multiple exceptions with |. finally always executes for cleanup. throw keyword throws an exception. throws keyword declares checked exceptions in method signature. Custom exceptions extend Exception or RuntimeException. Best practice: catch specific exceptions, log them properly, don't swallow exceptions silently."),
        ("What is Spring Framework?",
         "Spring is a comprehensive Java enterprise framework. Core features: IoC container manages object lifecycle and dependencies through Dependency Injection. Spring MVC for web applications following MVC pattern. Spring Data simplifies database access. Spring Security for authentication and authorization. Spring Boot auto-configures Spring applications reducing boilerplate. AOP for cross-cutting concerns like logging. Spring uses annotations like @Component, @Autowired, @RestController, @Service to configure applications."),
        ("What are design patterns? Name 3 commonly used ones.",
         "Design patterns are reusable solutions to common software design problems. Three important ones: Singleton - ensures only one instance of a class exists, used for database connections, configuration managers. Factory Method - creates objects without specifying exact class, promotes loose coupling. Observer - defines one-to-many dependency, when one object changes state all dependents are notified, used in event systems. Other important patterns: Strategy, Decorator, Adapter, Builder, MVC."),
        ("What is the difference between == and .equals() in Java?",
         "== operator compares references (memory addresses) for objects. For primitives it compares values. .equals() method compares content/values of objects. String class overrides equals() to compare character sequences. Without overriding equals(), it behaves like ==. Always use .equals() for String comparison. Override equals() and hashCode() together in custom classes for correct behavior in collections. == can give unexpected results with Integer caching for values -128 to 127."),
    ],
    "Testing/QA": [
        ("What is the difference between manual and automated testing?",
         "Manual testing involves human testers executing test cases without tools, good for exploratory testing, usability testing, and one-time tests. Automated testing uses tools like Selenium, JUnit to execute tests programmatically, good for regression testing, repetitive tasks, and performance testing. Automation has higher initial cost but lower long-term cost. Manual testing catches visual and UX issues better. Best practice is to automate stable, repetitive tests and use manual for exploratory and new features."),
        ("Explain the software testing life cycle (STLC).",
         "STLC consists of phases: Requirement Analysis - understand what to test, identify testable requirements. Test Planning - define scope, approach, resources, schedule, risk analysis. Test Case Design - create test cases, test data, test scripts. Test Environment Setup - configure hardware, software, test data. Test Execution - run tests, log defects, track results. Test Cycle Closure - evaluate completion criteria, metrics, lessons learned. Each phase has entry and exit criteria."),
        ("What is regression testing?",
         "Regression testing verifies that previously working functionality still works after code changes, bug fixes, or new features. It prevents software regression where fixed bugs reappear or new features break existing ones. Regression suite contains test cases for all critical functionality. Run after every code change. Automation is essential for regression testing to be cost-effective. Prioritize high-risk and frequently used features. Both smoke testing and full regression are part of regression strategy."),
        ("What are the different types of testing?",
         "Functional testing: Unit (individual components), Integration (between modules), System (complete system), Acceptance (user requirements). Non-functional testing: Performance, Load, Stress, Security, Usability. Structural testing: White-box testing examining code structure. Change-related: Regression, Re-testing after fixes, Smoke/Sanity. Other types: Exploratory, Ad-hoc, Monkey testing. Each type serves different purposes and should be applied appropriately based on risk and project needs."),
        ("How do you write effective test cases?",
         "Effective test cases should have: clear unique test case ID, descriptive title, preconditions and test data, clear step-by-step execution steps, expected results for each step, actual results section, pass/fail status. Follow principles: one verification per test case, be specific not vague, use boundary values, include positive and negative scenarios, make tests independent and repeatable. Review test cases with developers and business analysts. Maintain and update as requirements change."),
        ("What is Selenium and how does it work?",
         "Selenium is an open-source web automation framework. Selenium WebDriver controls browsers programmatically through browser-specific drivers (ChromeDriver, GeckoDriver). It supports multiple languages: Java, Python, C#, Ruby. Works by sending commands to browser driver which controls actual browser. Selenium Grid allows parallel execution on multiple machines and browsers. Page Object Model pattern organizes code. Limitations: only web apps, no built-in reporting, requires programming knowledge. Often combined with TestNG or JUnit."),
        ("Explain boundary value analysis and equivalence partitioning.",
         "Equivalence Partitioning divides input data into partitions where all values behave the same way. Test one value from each partition. Example: age field 18-60: partitions are <18, 18-60, >60. Boundary Value Analysis tests at the edges of partitions where bugs are most likely. For 18-60: test 17, 18, 19, 59, 60, 61. BVA extends equivalence partitioning by focusing on boundaries. Together they reduce test cases while maintaining good coverage. Both are black-box techniques."),
        ("What is API testing?",
         "API testing validates APIs at the business logic layer without UI. Test request/response, HTTP methods (GET, POST, PUT, DELETE), status codes, headers, authentication, error handling. Tools include Postman, REST Assured, SoapUI. Test cases: valid inputs, invalid inputs, authentication, authorization, error responses, response time, data format validation. API tests run faster than UI tests and are less brittle. Important for microservices. Contract testing ensures API consumers and providers agree on API contract."),
        ("How do you perform performance testing?",
         "Performance testing validates system behavior under expected and peak loads. Types: Load testing (expected load), Stress testing (beyond capacity to find breaking point), Spike testing (sudden load increase), Endurance/Soak testing (sustained load over time), Volume testing (large data). Tools: JMeter, Gatling, LoadRunner, k6. Key metrics: response time, throughput, error rate, resource utilization (CPU, memory). Identify bottlenecks, tune databases, optimize queries, configure caching. Establish baseline before optimization."),
        ("What is a bug life cycle?",
         "Bug life cycle tracks a defect from discovery to closure. Stages: New - bug reported and logged. Assigned - assigned to developer. In Progress - developer investigating. Fixed - developer applied fix. Ready for Testing/Retest - fix deployed to test environment. Retest - QA verifies the fix. Closed - fix verified, bug closed. Rejected - not a bug or by design. Deferred - postponed to later release. Duplicate - already reported. Each transition requires clear documentation and communication between QA and development."),
        ("What is the difference between smoke testing and sanity testing?",
         "Smoke testing is initial testing to check if basic build is stable enough for detailed testing. It tests the most critical functions (build verification test). Done after each build deployment. Sanity testing is a subset of regression testing done after minor changes or bug fixes to verify that specific functionality works. Smoke is broad and shallow, sanity is narrow and deep. Smoke checks if system starts and basic flows work. Sanity checks if a specific fix or feature works correctly."),
        ("How do you prioritize bugs?",
         "Bug prioritization uses severity (impact on functionality) and priority (business urgency). Severity levels: Critical (system crash, data loss), High (major feature broken), Medium (feature works with workaround), Low (cosmetic issues). Priority levels: P1 (fix immediately), P2 (fix in this release), P3 (fix in next release), P4 (fix when time permits). High severity doesn't always mean high priority and vice versa. Consider: customer impact, frequency of occurrence, workaround availability, release deadlines when prioritizing."),
    ],
    "DevOps": [
        ("What is CI/CD and why is it important?",
         "CI (Continuous Integration) is the practice of frequently merging code changes to a shared repository with automated builds and tests. CD can mean Continuous Delivery (automated deployment to staging, manual to production) or Continuous Deployment (fully automated to production). Benefits: faster feedback on bugs, reduced integration problems, consistent deployments, faster time to market. Tools: Jenkins, GitLab CI, GitHub Actions, CircleCI. Pipeline stages: build, test, security scan, deploy. CI/CD is fundamental to DevOps culture."),
        ("Explain Docker and containerization.",
         "Docker is a platform for containerization - packaging applications with all dependencies into isolated containers. Containers share the host OS kernel, making them lighter than VMs. Dockerfile defines how to build an image. Images are immutable, containers are running instances. Docker Hub stores public images. Key commands: docker build, docker run, docker pull, docker push. docker-compose orchestrates multi-container applications. Benefits: consistency across environments, isolation, fast startup, portability. Containers solve 'works on my machine' problem."),
        ("What is Kubernetes and how does it work?",
         "Kubernetes (K8s) is a container orchestration platform that automates deployment, scaling, and management of containerized applications. Architecture: Master node runs API server, scheduler, controller manager, etcd. Worker nodes run pods via kubelet. Pod is the smallest deployable unit, typically one container. Deployment manages replica sets for high availability. Service provides stable network endpoint. Ingress manages external access. ConfigMaps and Secrets for configuration. Key features: auto-scaling, self-healing, rolling updates, load balancing."),
        ("What is Infrastructure as Code (IaC)?",
         "IaC manages and provisions infrastructure using code and configuration files rather than manual processes. Benefits: version control for infrastructure, consistency, repeatability, collaboration, disaster recovery. Tools: Terraform (cloud-agnostic, declarative), AWS CloudFormation (AWS-specific), Ansible (configuration management), Pulumi (general purpose languages). Declarative approach defines desired state, tool figures out how to achieve it. Imperative approach defines steps. IaC enables treating infrastructure like application code with code reviews, testing, CI/CD."),
        ("Explain Git branching strategies.",
         "Common branching strategies: GitFlow has main, develop, feature, release, hotfix branches. Feature branches merge to develop, releases branch from develop, hotfix branches from main. Trunk-based development uses short-lived feature branches merged frequently to main/trunk, uses feature flags. GitHub Flow is simpler - feature branches, PR to main, deploy after merge. Choose based on release frequency and team size. GitFlow suits scheduled releases. Trunk-based suits continuous deployment. Whatever strategy, keep branches short-lived to avoid merge conflicts."),
        ("What is the difference between Chef, Puppet, and Ansible?",
         "All three are configuration management tools. Chef uses Ruby DSL, follows client-server model, uses recipes and cookbooks, steep learning curve. Puppet also client-server, uses its own declarative language (Puppet DSL), good for large scale. Ansible is agentless (uses SSH), uses YAML playbooks, push-based, easiest to learn. Chef and Puppet are pull-based (agents pull config from server). Ansible is push-based. For most modern teams, Ansible is preferred for its simplicity. Chef and Puppet better for very large, complex environments."),
        ("How do you monitor applications in production?",
         "Monitoring strategy: Metrics (CPU, memory, response time, error rates) with Prometheus, Datadog, CloudWatch. Logging (application logs, access logs) with ELK stack (Elasticsearch, Logstash, Kibana) or Grafana Loki. Tracing (request flow across services) with Jaeger or Zipkin. Alerting: set thresholds, alert on anomalies, route to correct team. Dashboards in Grafana for visualization. Key metrics: RED (Rate, Errors, Duration), USE (Utilization, Saturation, Errors). Health checks and synthetic monitoring. On-call runbooks for common issues."),
        ("What is blue-green deployment?",
         "Blue-green deployment maintains two identical production environments (blue and green). Blue is currently live, green is the new version. Deploy new version to green, run smoke tests, then switch traffic from blue to green via load balancer or DNS. If issues arise, instantly rollback by switching traffic back to blue. Blue becomes the new idle environment for next deployment. Benefits: zero downtime deployment, instant rollback, production testing. Requires double the infrastructure cost. Variant: canary deployment gradually shifts percentage of traffic to new version."),
        ("Explain microservices architecture.",
         "Microservices breaks monolithic application into small independent services each running in its own process and communicating via APIs. Each service owns its data and business logic. Benefits: independent deployment, technology flexibility, fault isolation, team autonomy, easier scaling of individual services. Challenges: distributed system complexity, network latency, data consistency, service discovery, increased operational overhead. Use API Gateway as single entry point. Service mesh (Istio) for service-to-service communication. Event-driven communication with message queues (Kafka, RabbitMQ)."),
        ("What is Jenkins and how do you use it?",
         "Jenkins is an open-source automation server for CI/CD. It automates building, testing, and deploying applications. Pipeline as code using Jenkinsfile (Groovy). Key concepts: Job/Project (unit of work), Build (execution of job), Pipeline (series of stages), Agent (where builds run), Stage (logical group of steps). Plugins ecosystem extends functionality. Master-agent architecture for distributed builds. Declarative pipeline is simpler, scripted pipeline more flexible. Alternatives: GitLab CI, GitHub Actions, CircleCI. Jenkins is highly customizable but requires maintenance."),
        ("How do you handle secrets management in DevOps?",
         "Never store secrets in code or version control. Use dedicated secrets management tools: HashiCorp Vault provides centralized secrets storage with access control, audit logging, dynamic secrets. Cloud-native options: AWS Secrets Manager, Azure Key Vault, GCP Secret Manager. Kubernetes Secrets (base64 encoded, not truly secret - use sealed secrets or Vault integration). Environment variables injected at runtime. Rotate secrets regularly. Principle of least privilege - services only access secrets they need. Audit access to secrets. Use different secrets per environment."),
        ("What is the difference between horizontal and vertical scaling?",
         "Vertical scaling (scale up) means adding more resources to existing server - more CPU, RAM, storage. Simple but has limits and single point of failure. Requires downtime usually. Horizontal scaling (scale out) means adding more servers to distribute load. More complex but virtually limitless, better fault tolerance, no single point of failure. Stateless applications scale horizontally easily. Stateful apps need session management (Redis) for horizontal scaling. Auto-scaling groups in cloud automatically adjust number of instances based on load. Modern cloud-native apps favor horizontal scaling."),
    ],
    "Full Stack Developer": [
        ("What is the difference between REST and GraphQL?",
         "REST uses fixed endpoints for each resource, returns fixed data structure. Can lead to over-fetching or under-fetching. Multiple endpoints needed for related data. GraphQL uses single endpoint, client specifies exactly what data it needs in query. Eliminates over/under-fetching. Better for complex, interconnected data. REST is simpler, well-understood, better caching. GraphQL is more flexible, great for mobile where bandwidth matters. REST uses HTTP methods semantically. GraphQL uses POST for queries. Choose REST for simple CRUD, GraphQL for complex data requirements."),
        ("Explain the MVC architecture pattern.",
         "MVC separates application into three components. Model manages data, business logic, and rules - interacts with database. View handles display/presentation - renders data from model to user. Controller handles user input, orchestrates model and view - processes requests, updates model, selects view. Benefits: separation of concerns, easier testing, code reusability, parallel development. In web: URL routing maps to controller, controller fetches from model, passes to view template. React uses component-based which blends V and C. Django and Rails are MVC frameworks."),
        ("What is the difference between SQL and NoSQL databases?",
         "SQL databases are relational, use structured tables with predefined schema, support ACID transactions, use SQL query language. Good for structured data with clear relationships. Examples: MySQL, PostgreSQL. NoSQL databases are non-relational, flexible schema, horizontal scaling. Types: Document (MongoDB), Key-Value (Redis), Column (Cassandra), Graph (Neo4j). NoSQL trades ACID for performance and scalability. Choose SQL for complex queries, transactions, structured data. NoSQL for large-scale, unstructured data, high throughput, flexible schema."),
        ("What are React hooks and why were they introduced?",
         "React hooks are functions that let functional components use state and lifecycle features previously only in class components. Introduced in React 16.8 to simplify component logic and reuse stateful logic. Key hooks: useState for local state, useEffect for side effects (like componentDidMount/Update), useContext for consuming context, useReducer for complex state, useMemo and useCallback for performance optimization. Custom hooks extract reusable stateful logic. Hooks solved problems with class components: complex lifecycle methods, difficulty reusing stateful logic, confusing 'this' keyword."),
        ("Explain how JWT authentication works.",
         "JWT (JSON Web Token) consists of three parts: Header (algorithm and token type), Payload (claims - user data), Signature (verification). Process: user logs in with credentials, server verifies and creates JWT signed with secret key, sends JWT to client, client stores in localStorage or cookie, includes JWT in Authorization header for subsequent requests, server verifies signature on each request without database lookup. Stateless - server doesn't store sessions. Tokens have expiry. Refresh tokens for long sessions. HTTPS required to prevent interception."),
        ("What is CORS and how do you handle it?",
         "CORS (Cross-Origin Resource Sharing) is a browser security mechanism that restricts web pages from making requests to different domains than the one served. When frontend (localhost:3000) calls backend API (localhost:5000), browser checks if server allows cross-origin. Server must include Access-Control-Allow-Origin header. Preflight OPTIONS request for non-simple requests. Handle in backend: for Flask use flask-cors, for Express use cors middleware. Configure allowed origins, methods, headers. For development allow all origins. For production specify exact frontend domain for security."),
        ("What is the event loop in JavaScript?",
         "JavaScript is single-threaded with an event loop for async operations. Call stack executes synchronous code. Web APIs handle async operations (setTimeout, fetch, DOM events) outside the stack. Callback queue holds completed async callbacks. Microtask queue (higher priority) holds Promise callbacks. Event loop continuously checks if call stack is empty, processes all microtasks first, then processes one macro task from callback queue. This enables non-blocking I/O. async/await is syntactic sugar over Promises, making async code look synchronous."),
        ("Explain database indexing.",
         "Indexes improve query performance by creating separate data structures that allow faster lookup. Like a book index - avoid scanning entire table. B-tree indexes work for range queries and equality. Hash indexes for equality only. Composite indexes on multiple columns. Primary key is automatically indexed. Create indexes on frequently queried columns, foreign keys, columns in WHERE/JOIN/ORDER BY. Downsides: slower writes (index must be updated), storage space. Too many indexes hurt write performance. Use EXPLAIN to analyze query execution plans. Index cardinality matters - high cardinality columns benefit more."),
        ("What is Docker and why use it in development?",
         "Docker packages applications and dependencies into containers ensuring consistent environments. Benefits for full stack: frontend, backend, database each in containers. docker-compose orchestrates all services with one command. Eliminates 'works on my machine' - everyone uses same environment. Easy onboarding - new developers run docker-compose up. Environment parity - dev matches staging/production. Isolated databases per project. Easy to switch Node/Python versions. Volumes for persistent data. Networks for service communication. Dockerfile defines build, docker-compose.yml defines multi-service setup."),
        ("What is the difference between authentication and authorization?",
         "Authentication verifies identity - who are you? User provides credentials (password, biometric, token), system verifies they are who they claim. Results in authenticated identity. Authorization determines permissions - what can you do? After authentication, system checks if user has permission to access resource or perform action. Role-based access control (RBAC) assigns permissions to roles. Authentication always happens before authorization. Example: Login is authentication. Checking if logged-in user can access admin panel is authorization. JWT handles authentication, permissions/roles handle authorization."),
        ("How does a browser render a webpage?",
         "Browser rendering pipeline: DNS lookup resolves domain to IP. TCP connection established. HTTP request sent, response received. HTML parsing builds DOM tree. CSS parsing builds CSSOM tree. Render tree combines DOM and CSSOM (only visible elements). Layout (reflow) calculates position and size of each element. Paint converts render tree to pixels. Composite layers in correct order. JavaScript can modify DOM causing reflow/repaint. Performance optimization: minimize reflows, use CSS transforms (composite layer), lazy load images, code splitting, browser caching."),
        ("What are WebSockets and when do you use them?",
         "WebSockets provide full-duplex communication channel over single TCP connection, unlike HTTP which is request-response. After initial HTTP handshake, connection stays open for real-time bidirectional communication. Use cases: chat applications, live notifications, collaborative editing (Google Docs style), real-time dashboards, online gaming, live stock prices. Socket.io library simplifies WebSocket implementation with fallbacks. Server pushes data to client without client requesting. Compare to HTTP polling (repeated requests) - WebSockets are more efficient. Use HTTP for standard CRUD, WebSockets for real-time features."),
    ],
    "Frontend Developer": [
        ("What is the difference between CSS Flexbox and Grid?",
         "Flexbox is one-dimensional - works along a single axis (row or column). Best for distributing items in a line, navbar items, centering elements. Properties: display:flex, flex-direction, justify-content, align-items, flex-wrap, gap. Grid is two-dimensional - works in both rows and columns simultaneously. Best for page layouts, complex arrangements. Properties: display:grid, grid-template-columns/rows, grid-area, gap. Use Flexbox for component-level layout (navigation, button groups). Use Grid for page-level layout (overall page structure). They complement each other and can be nested."),
        ("Explain the CSS box model.",
         "Every HTML element is a rectangular box. Box model layers from inside out: Content (actual text/images, width/height). Padding (space between content and border, transparent, takes background color). Border (outline around padding, has width/style/color). Margin (space outside border, transparent). box-sizing: content-box (default) - width applies to content only. box-sizing: border-box - width includes padding and border, makes sizing intuitive. Understanding box model is essential for layout. margin: auto centers block elements horizontally."),
        ("What is the virtual DOM in React?",
         "Virtual DOM is an in-memory representation of the real DOM. When state changes, React creates new virtual DOM tree, compares with previous (diffing algorithm - reconciliation), calculates minimum changes needed (patching), updates real DOM only where needed. Real DOM manipulation is expensive - causes reflow and repaint. Virtual DOM batches updates and minimizes real DOM operations. React 18 introduces concurrent features for even better performance. Vue also uses virtual DOM. Svelte compiles to vanilla JS without virtual DOM. Virtual DOM is an optimization not a requirement."),
        ("What are CSS preprocessors and their advantages?",
         "CSS preprocessors extend CSS with programming features and compile to regular CSS. SASS/SCSS: variables ($primary-color), nesting (write selectors inside parent), mixins (reusable blocks of CSS), functions, inheritance (@extend). LESS: similar features. PostCSS: transforms CSS with plugins (Autoprefixer adds vendor prefixes). Advantages: variables reduce repetition, nesting matches HTML structure, mixins avoid duplication, partials split CSS into files. Modern CSS variables (custom properties) reduce some need for preprocessors. Most projects use SCSS or PostCSS."),
        ("Explain event bubbling and event capturing in JavaScript.",
         "Events in DOM propagate in two phases. Capturing phase (top-down): event travels from window down to target element. Bubbling phase (bottom-up): event bubbles from target element up to window. By default, event listeners use bubbling phase. addEventListener third parameter: false (bubbling, default) or true (capturing). event.stopPropagation() stops propagation. event.preventDefault() prevents default browser behavior. Event delegation: attach single listener to parent instead of many children - uses bubbling. Efficient for dynamic lists. Useful pattern for performance optimization."),
        ("What is JavaScript closure?",
         "A closure is a function that remembers its lexical scope even when executed outside that scope. Inner function has access to outer function's variables after outer function has returned. Created every time a function is created. Use cases: data privacy (module pattern), factory functions, memoization, partial application. Example: function counter() { let count=0; return function() { return ++count; }} - returned function closes over count variable. Common interview gotcha with loops and var vs let. Closures are fundamental to JavaScript - callbacks, event handlers all use closures."),
        ("What is responsive web design?",
         "Responsive web design makes websites work well on all screen sizes from mobile to desktop. Techniques: Fluid grid layouts using percentages not fixed pixels. Flexible images (max-width: 100%). CSS media queries apply different styles at breakpoints. Mobile-first approach: design for mobile then enhance for larger screens. Bootstrap and Tailwind provide responsive grid systems. Common breakpoints: 576px (mobile), 768px (tablet), 992px (laptop), 1200px (desktop). Viewport meta tag required for mobile rendering. Test with browser DevTools device emulation. Consider touch targets for mobile."),
        ("What is the difference between localStorage, sessionStorage, and cookies?",
         "localStorage: stores data with no expiration, persists after browser close, ~5-10MB, accessible by same origin. sessionStorage: stores data for session duration, cleared when tab closes, ~5MB, same origin only. Cookies: ~4KB, can set expiration, sent automatically with every HTTP request (bandwidth overhead), accessible by server and client, HttpOnly flag prevents JS access for security, SameSite and Secure flags for CSRF protection. Use localStorage for user preferences, dark mode. sessionStorage for single-session data. Cookies for authentication tokens (with HttpOnly flag)."),
        ("Explain the difference between == and === in JavaScript.",
         "=== (strict equality) compares both value and type without type conversion. 0 === false is false because types differ. == (loose equality) performs type coercion before comparison. 0 == false is true because false is coerced to 0. Type coercion rules are complex and unintuitive. Always use === to avoid bugs. Common surprises with ==: null == undefined is true, 1 == '1' is true, [] == false is true. Linters (ESLint) enforce ===. The only valid use case for == is null checks: value == null checks for both null and undefined."),
        ("What is TypeScript and why use it?",
         "TypeScript is JavaScript with static type checking, developed by Microsoft. Adds types to variables, function parameters, return values. Catches type errors at compile time rather than runtime. Features: interfaces and types define shapes, generics for reusable typed code, enums, decorators, better IDE support with autocomplete and refactoring. Compiles to JavaScript. Benefits: fewer runtime bugs, better documentation through types, confident refactoring in large codebases. Learning curve exists but worth it for medium-large projects. All major frameworks have TypeScript support. Required for many professional jobs."),
        ("What is code splitting in webpack?",
         "Code splitting divides bundle into smaller chunks loaded on demand rather than single large bundle. Reduces initial load time. Methods: Entry points (manual splitting), Dynamic imports (import() syntax for lazy loading), SplitChunksPlugin (automatic vendor chunk splitting). React.lazy() and Suspense for component-level code splitting. Route-based splitting is most common - each route loads its own chunk. Vendor splitting separates rarely-changing third-party libraries for better caching. Analyze bundle with webpack-bundle-analyzer. Tree shaking removes unused code. Both reduce bundle size improving performance."),
        ("What are Web Accessibility (a11y) best practices?",
         "Web accessibility ensures websites work for people with disabilities. WCAG guidelines provide standards. Key practices: Semantic HTML (use button not div for buttons, header/nav/main/footer). Alt text for images. Sufficient color contrast (4.5:1 for normal text). Keyboard navigation support - all interactive elements focusable. ARIA labels for elements without visible text. Focus indicators visible. Form labels associated with inputs. Skip navigation links. Proper heading hierarchy (h1, h2, h3 in order). Screen reader testing with NVDA or VoiceOver. Accessibility is legal requirement in many countries and improves SEO."),
    ],
    "Backend Developer": [
        ("What is RESTful API design and its principles?",
         "REST (Representational State Transfer) is architectural style for APIs. Principles: Stateless - each request contains all information needed, no server-side sessions. Client-Server separation. Cacheable responses. Uniform interface: resources identified by URLs, manipulation through representations. Layered system. Code on demand (optional). Best practices: use nouns for URLs (/users not /getUsers), HTTP methods correctly (GET read, POST create, PUT/PATCH update, DELETE remove), proper status codes (200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Server Error), versioning (/api/v1/users), pagination for lists."),
        ("Explain database transactions and ACID properties.",
         "Transaction is a sequence of operations treated as single unit. ACID properties ensure reliability. Atomicity: all operations succeed or all fail, no partial transactions. Consistency: database moves from one valid state to another, constraints maintained. Isolation: concurrent transactions execute as if sequential, isolation levels (Read Uncommitted, Read Committed, Repeatable Read, Serializable). Durability: committed transactions persist even after crash. Use transactions for operations that must succeed together (bank transfer: debit one account, credit another). BEGIN TRANSACTION, COMMIT, ROLLBACK. ORM frameworks provide transaction management."),
        ("What is the N+1 query problem?",
         "N+1 problem occurs when code executes one query to fetch N records, then N additional queries for each record's related data. Example: fetch 100 users, then for each user fetch their orders = 101 queries. Causes severe performance degradation. Solutions: Eager loading with JOIN queries fetches all data in one query. In ORMs: Django's select_related() and prefetch_related(), SQLAlchemy's joinedload(). DataLoader pattern in GraphQL batches and deduplicates queries. Always monitor query count during development. Use Django Debug Toolbar or SQLAlchemy query logging to detect N+1 problems."),
        ("What are middleware in backend frameworks?",
         "Middleware are functions that execute between receiving request and sending response. They can modify request/response objects, execute code, end request-response cycle, or call next middleware. Common middleware: Authentication (verify JWT token), Authorization (check permissions), Logging (log request details), CORS (add headers), Rate limiting (prevent abuse), Body parser (parse JSON/form data), Error handling. In Express: app.use(middleware). In Django: MIDDLEWARE list in settings. Middleware chains allow composing request processing pipeline. Order matters - auth middleware before route handlers."),
        ("Explain caching strategies in backend development.",
         "Caching stores frequently accessed data for faster retrieval. Strategies: Cache-aside (lazy loading) - application checks cache, on miss loads from DB and caches. Write-through - write to cache and DB simultaneously. Write-behind - write to cache, async write to DB. Read-through - cache handles DB interaction. Levels: In-memory cache (application level, fastest), Distributed cache (Redis, Memcached - shared across servers), CDN cache (static assets), Database query cache, HTTP caching (Cache-Control headers). Cache invalidation is hard - TTL (time-to-live), event-based invalidation. Redis for session storage, rate limiting, pub/sub."),
        ("What is rate limiting and how do you implement it?",
         "Rate limiting restricts number of requests a user/IP can make in a time window. Prevents abuse, DDoS attacks, ensures fair usage. Algorithms: Fixed Window (count requests in fixed time window, simple but allows burst at window boundary), Sliding Window (more accurate, tracks request timestamps), Token Bucket (tokens added at rate, consumed per request, allows bursts), Leaky Bucket (requests processed at constant rate). Implementation: Redis with atomic operations, nginx rate limiting module, API gateway rate limiting. Return 429 Too Many Requests with Retry-After header. Different limits for different endpoints and user tiers."),
        ("What is database connection pooling?",
         "Connection pooling maintains a pool of reusable database connections rather than creating new connection for each request. Creating connections is expensive (authentication, SSL handshake, network round trips). Pool is pre-initialized at startup. Requests borrow connection, execute query, return to pool. Benefits: reduced latency, resource efficiency, connection limit management. Configuration: min/max pool size, connection timeout, idle timeout. Tools: PgBouncer for PostgreSQL, HikariCP for Java, SQLAlchemy pool for Python. Too small pool causes queuing, too large overwhelms database. Monitor pool utilization to tune size."),
        ("Explain message queues and when to use them.",
         "Message queues enable asynchronous communication between services. Producer puts messages in queue, consumer processes independently. Benefits: decoupling (producer doesn't wait for consumer), reliability (messages persisted if consumer down), scalability (add consumers for high load), spike handling (queue absorbs traffic bursts). Use cases: email sending, image processing, order processing, notifications. Tools: RabbitMQ (traditional message broker, AMQP protocol), Apache Kafka (high-throughput event streaming, message retention), AWS SQS, Redis pub/sub. Patterns: work queues (competing consumers), pub/sub (fanout), routing."),
        ("What is the difference between synchronous and asynchronous programming?",
         "Synchronous: code executes sequentially, each operation waits for previous to complete. Simple to understand but blocks on I/O operations. Asynchronous: operations can start without waiting for others to complete, callbacks/promises/async-await notify when done. Essential for I/O-bound operations (database calls, API calls, file operations). Node.js event loop enables async. Python asyncio for async. Benefits: better resource utilization (thread not blocked waiting), higher throughput. Async with async/await syntax is as readable as sync code. Use async for I/O-bound work, multiprocessing for CPU-bound work."),
        ("What is GraphQL and its advantages over REST?",
         "GraphQL is a query language for APIs and runtime for executing queries. Client specifies exactly what data it needs. Single endpoint vs REST's multiple endpoints. Advantages: no over-fetching (get only requested fields), no under-fetching (get related data in one query), strongly typed schema as contract, introspection for self-documentation, great for mobile (less data transfer). Disadvantages: complex caching (not HTTP cache friendly), N+1 problem requires DataLoader, file uploads require workarounds, overkill for simple CRUD APIs. Best for complex, interconnected data models and when multiple clients need different data shapes."),
        ("How do you handle errors in a backend API?",
         "Error handling strategy: Use proper HTTP status codes (400 series client errors, 500 series server errors). Return consistent error response format: {error: {code: 'VALIDATION_ERROR', message: 'Email is invalid', details: []}}. Centralized error handling middleware catches unhandled errors. Distinguish operational errors (invalid input, not found - expected) from programmer errors (bugs - should crash and restart). Log all errors with context: request ID, user ID, stack trace. Never expose internal error details to clients in production. Use correlation IDs to trace requests across services. Alert on 5xx error rate spikes."),
        ("What is containerization and Docker best practices?",
         "Docker best practices for backend: Use official base images, prefer slim/alpine variants for smaller size. Multi-stage builds - separate build environment from runtime. Don't run as root user in container. One process per container. Use .dockerignore to exclude unnecessary files. Pin image versions for reproducibility. Store secrets as environment variables or secrets manager, never in image. Health checks in Dockerfile. Minimize layers by combining RUN commands. Use COPY not ADD. Non-root user for security. Keep containers stateless - state in volumes or external services. Resource limits (memory, CPU) in compose or kubernetes specs."),
    ],
    "Data Science": [
        ("What is the difference between supervised and unsupervised learning?",
         "Supervised learning uses labeled training data where correct answers are known. Model learns mapping from input to output. Types: Classification (predict category - spam/not spam) and Regression (predict continuous value - house price). Algorithms: Linear Regression, Decision Trees, SVM, Neural Networks. Unsupervised learning has no labels, finds hidden patterns. Types: Clustering (K-Means groups similar data), Dimensionality Reduction (PCA reduces features), Association (market basket analysis). Supervised is more common in production. Unsupervised for exploration. Semi-supervised uses small labeled and large unlabeled data. Reinforcement learning learns from environment rewards."),
        ("Explain bias-variance tradeoff.",
         "Bias is error from wrong assumptions in learning algorithm. High bias = underfitting - model too simple, fails to capture patterns. Example: fitting straight line to curved data. Variance is error from sensitivity to fluctuations in training data. High variance = overfitting - model memorizes training data, fails on new data. Tradeoff: decreasing bias increases variance and vice versa. Goal is to find sweet spot with good generalization. Techniques to reduce overfitting: regularization (L1/L2), dropout, early stopping, cross-validation, more training data. Techniques to reduce underfitting: more complex model, more features, reduce regularization."),
        ("What is cross-validation and why is it important?",
         "Cross-validation is a technique for evaluating model performance on unseen data. K-fold CV splits data into K folds, trains on K-1 folds, tests on 1 fold, repeats K times, averages results. Stratified K-fold maintains class distribution in each fold for classification. Leave-one-out CV uses single sample as test set. Benefits: more reliable estimate than single train/test split, uses all data for both training and testing, detects overfitting. Helps tune hyperparameters without test set contamination. Always hold out final test set that model never sees. Prevents overfitting to validation set during model selection."),
        ("What are the steps in a typical data science project?",
         "Data science workflow: Problem Definition - understand business problem, define success metrics. Data Collection - gather from databases, APIs, web scraping, surveys. Exploratory Data Analysis (EDA) - understand distributions, correlations, anomalies with visualizations. Data Preprocessing - handle missing values, outliers, encode categoricals, normalize/standardize features. Feature Engineering - create new meaningful features from existing ones. Model Selection - try multiple algorithms, baseline models first. Training and Validation - cross-validation, hyperparameter tuning. Evaluation - metrics appropriate to problem (accuracy, F1, RMSE, AUC). Deployment - productionize model. Monitoring - track model performance over time."),
        ("What is feature engineering?",
         "Feature engineering creates new features or transforms existing ones to improve model performance. Techniques: Mathematical transformations (log, square root for skewed data), binning continuous variables into categories, interaction features (multiply/divide features), polynomial features, date/time features (extract hour, day, month, is_weekend), text features (TF-IDF, word embeddings), aggregations (mean, sum, count of related records), lag features for time series, target encoding for high-cardinality categoricals. Domain knowledge is crucial for meaningful features. Feature selection removes irrelevant features using correlation analysis, feature importance, recursive feature elimination. Good features often matter more than algorithm choice."),
        ("Explain common evaluation metrics for classification.",
         "Accuracy: correct predictions / total predictions. Misleading for imbalanced classes. Precision: true positives / (true positives + false positives) - of predicted positives, how many are actually positive. Recall/Sensitivity: true positives / (true positives + false negatives) - of actual positives, how many were caught. F1 Score: harmonic mean of precision and recall. ROC-AUC: area under ROC curve, model's ability to discriminate classes, threshold-independent. Choose metric based on problem: email spam (high precision), cancer detection (high recall), balanced F1. Confusion matrix shows breakdown. For multi-class: macro average treats classes equally, weighted average accounts for class imbalance."),
        ("What is regularization in machine learning?",
         "Regularization adds penalty term to loss function to prevent overfitting by discouraging complex models. L1 (Lasso) regularization adds sum of absolute values of weights. Promotes sparsity - drives some weights to exactly zero, performs feature selection. L2 (Ridge) regularization adds sum of squared weights. Shrinks all weights but rarely to zero. Better when all features relevant. Elastic Net combines L1 and L2. Lambda/alpha hyperparameter controls regularization strength - higher value = stronger regularization. Dropout in neural networks randomly deactivates neurons during training. Data augmentation for image models. Weight decay in optimization."),
        ("What is the difference between bagging and boosting?",
         "Both are ensemble methods combining multiple models. Bagging (Bootstrap Aggregating): trains models independently in parallel on random subsets of data (with replacement), averages predictions (regression) or votes (classification). Reduces variance. Random Forest is bagging with decision trees. Boosting: trains models sequentially where each model focuses on previous model's errors. Reduces bias and variance. Examples: AdaBoost, Gradient Boosting (XGBoost, LightGBM). XGBoost is often winning algorithm for structured data in competitions. Bagging more robust to outliers. Boosting typically achieves better accuracy but more prone to overfitting and slower to train."),
        ("What is PCA (Principal Component Analysis)?",
         "PCA is dimensionality reduction technique. Finds directions (principal components) of maximum variance in data. Components are orthogonal to each other. Projects high-dimensional data to lower dimensions while preserving maximum variance. Process: standardize data, compute covariance matrix, compute eigenvectors and eigenvalues, sort by eigenvalue, select top k components. Applications: visualize high-dimensional data (reduce to 2D/3D), remove noise, reduce features for downstream models, handle multicollinearity. Scree plot shows variance explained by each component. Choose components that explain ~95% variance. Limitation: components are linear combinations, harder to interpret."),
        ("Explain the concept of a data pipeline.",
         "Data pipeline automates movement and transformation of data from source to destination. Stages: Ingestion (collect data from databases, APIs, files, streams), Transformation (clean, filter, aggregate, enrich data), Storage (data warehouse, data lake), Analytics (BI tools, machine learning). Tools: Apache Airflow for orchestration (DAGs define workflow), Apache Spark for large-scale processing, dbt for SQL transformations, Kafka for streaming. ETL (Extract Transform Load) vs ELT (Extract Load Transform - transform in warehouse). Batch processing for historical data. Stream processing for real-time data (Spark Streaming, Flink). Monitoring for pipeline failures and data quality."),
        ("What is the difference between mean, median, and mode?",
         "Mean is arithmetic average - sum of values divided by count. Sensitive to outliers. Use for symmetric distributions. Median is middle value when sorted. Robust to outliers. Better for skewed distributions (income, house prices). Mode is most frequent value. Used for categorical data. Useful for finding common patterns. Example: dataset [1, 2, 2, 3, 100]. Mean=21.6 (skewed by outlier), Median=2 (more representative), Mode=2. Right-skewed distribution (income): mean > median > mode. Left-skewed: mode > median > mean. In practice, always look at the distribution before choosing summary statistic. Variance and standard deviation measure spread."),
        ("What tools and libraries do you use for data science in Python?",
         "Core Python data science stack: NumPy for numerical computing, array operations, linear algebra. Pandas for data manipulation, DataFrames, cleaning, merging. Matplotlib and Seaborn for visualization. Scikit-learn for machine learning: preprocessing, model selection, evaluation metrics, classical ML algorithms. XGBoost/LightGBM for gradient boosting. TensorFlow and PyTorch for deep learning. Jupyter Notebooks for interactive exploration. SQL for database queries. Statsmodels for statistical analysis. Plotly for interactive visualizations. Apache Spark with PySpark for big data. MLflow for experiment tracking and model registry. Great Expectations for data validation."),
    ],
    "Cybersecurity": [
        ("What is the CIA triad in cybersecurity?",
         "CIA Triad is fundamental model for information security. Confidentiality: information only accessible to authorized individuals. Protects sensitive data from unauthorized access. Controls: encryption, access controls, authentication, need-to-know principle. Integrity: data is accurate, complete, and not tampered with. Controls: checksums, hashing (SHA-256), digital signatures, audit logs, version control. Availability: systems and data accessible to authorized users when needed. Controls: redundancy, backups, DDoS mitigation, disaster recovery, high availability architecture. All three must be balanced - security measures protecting confidentiality can impact availability."),
        ("What is the difference between symmetric and asymmetric encryption?",
         "Symmetric encryption uses same key for encryption and decryption. Fast, efficient for large data. Problem: key distribution - how to securely share key? Examples: AES (industry standard), DES (outdated). Key sizes: AES-128, AES-256. Asymmetric encryption uses key pair: public key encrypts, private key decrypts. Anyone can encrypt with public key, only key owner can decrypt. Solves key distribution problem. Slower than symmetric. Examples: RSA, ECC. Used for: HTTPS handshake, digital signatures, SSH. In practice: asymmetric encryption securely exchanges symmetric key, symmetric encryption encrypts data (hybrid approach - TLS does this)."),
        ("What is SQL injection and how do you prevent it?",
         "SQL injection is attack where malicious SQL code is inserted into application queries. Example: username input: ' OR '1'='1 modifies WHERE clause to always be true, bypassing authentication. Can read, modify, delete database data. Prevention: Parameterized queries/prepared statements - treat user input as data, not SQL code. ORMs by default use parameterized queries. Input validation and sanitization. Principle of least privilege - database user should only have minimum permissions needed. Web Application Firewall (WAF). Stored procedures with caution. Never concatenate user input into SQL strings. Regular security testing. OWASP top 10 lists SQL injection as critical vulnerability."),
        ("Explain OWASP Top 10 vulnerabilities.",
         "OWASP Top 10 (2021) most critical web security risks: Broken Access Control (most common) - users can access unauthorized resources. Cryptographic Failures - weak encryption, data exposed in transit/rest. Injection - SQL, NoSQL, command injection. Insecure Design - security not designed in. Security Misconfiguration - default credentials, unnecessary features enabled. Vulnerable and Outdated Components - using libraries with known vulnerabilities. Identification and Authentication Failures - weak passwords, session management. Software and Data Integrity Failures - CI/CD pipeline compromised. Security Logging and Monitoring Failures - insufficient logging. Server-Side Request Forgery (SSRF) - server makes requests to unintended locations."),
        ("What is a man-in-the-middle attack?",
         "MITM attack occurs when attacker secretly intercepts and relays communication between two parties who believe they are communicating directly. Attacker can eavesdrop, modify messages. Scenarios: ARP poisoning on local network, DNS spoofing, evil twin WiFi access point, SSL stripping. Defenses: HTTPS/TLS encrypts traffic making interception useless. Certificate pinning prevents accepting fraudulent certificates. HSTS forces HTTPS connections. VPN encrypts all traffic. Avoid public WiFi or use VPN. Mutual TLS (mTLS) both parties authenticate. DNSSEC prevents DNS spoofing. Certificate Transparency logs detect fraudulent certificates."),
        ("What is penetration testing?",
         "Penetration testing is authorized simulated cyberattack to identify security vulnerabilities before malicious hackers do. Types: Black box (no prior knowledge), White box (full knowledge including source code), Grey box (partial knowledge). Phases: Planning (scope, rules of engagement), Reconnaissance (gather information - passive and active), Scanning (identify open ports, services, vulnerabilities), Exploitation (attempt to exploit findings), Post-exploitation (determine impact), Reporting (findings, severity, remediation). Tools: Nmap (scanning), Metasploit (exploitation), Burp Suite (web testing), Wireshark (packet capture). Always get written authorization. OWASP Testing Guide provides methodology."),
        ("What is zero trust security model?",
         "Zero trust principle: never trust, always verify. Traditional perimeter security trusted everything inside network - compromised internal system could access everything. Zero trust assumes breach and verifies every request regardless of network location. Principles: Verify explicitly (authenticate/authorize based on all data points), Use least privilege access (limit access to minimum needed), Assume breach (minimize blast radius, segment network). Implementation: Strong identity verification (MFA), device health verification, micro-segmentation, encrypt all traffic, monitor everything with analytics. Technologies: IAM, MFA, PAM, endpoint security, SIEM. Zero trust is architecture, not single product."),
        ("Explain how HTTPS and SSL/TLS works.",
         "HTTPS = HTTP + TLS (Transport Layer Security, formerly SSL). TLS handshake process: Client Hello (supported TLS versions, cipher suites, random number). Server Hello (chosen cipher suite, server certificate, random number). Certificate verification - client verifies certificate against trusted CAs. Key exchange - generate session keys using asymmetric crypto. Finished messages with MAC to verify integrity. Session begins using symmetric encryption with session keys. Certificate contains: domain name, public key, CA signature, expiry. Certificate Authorities (CAs) vouch for certificate authenticity. HSTS header tells browser to always use HTTPS. TLS 1.3 is current version, TLS 1.0/1.1 deprecated."),
        ("What is multi-factor authentication (MFA)?",
         "MFA requires two or more verification factors to authenticate. Factors: Something you know (password, PIN), Something you have (phone, hardware token, smart card), Something you are (fingerprint, face recognition, voice). Two-factor authentication (2FA) is most common MFA. Methods: SMS OTP (convenient but vulnerable to SIM swapping), Authenticator apps (TOTP - Google Authenticator, stronger than SMS), Hardware tokens (YubiKey - strongest), Push notifications (Duo Security), Biometrics. MFA dramatically reduces account compromise risk - even if password stolen, attacker needs second factor. Implement MFA especially for privileged accounts and sensitive operations. Backup codes for account recovery."),
        ("What is a firewall and what are its types?",
         "Firewall monitors and controls network traffic based on security rules. Types: Packet filtering - inspects packet headers (source/destination IP, ports, protocol), stateless, fast but limited. Stateful firewall - tracks connection state, understands context of traffic, more secure. Application layer firewall / WAF - inspects application layer content (HTTP), can detect SQL injection, XSS. Next-generation firewall (NGFW) - combines traditional firewall with IPS, application awareness, user identity. Host-based vs Network-based firewalls. DMZ (Demilitarized Zone) places public-facing servers between two firewalls. Defense in depth uses multiple firewall layers. Cloud security groups act as virtual firewalls."),
        ("What is incident response?",
         "Incident response is organized approach to handling security breaches or attacks. NIST framework phases: Preparation (policies, tools, training, playbooks before incident). Detection and Analysis (identify and confirm incident, assess scope and impact, preserve evidence). Containment (stop spread - short-term and long-term containment strategies). Eradication (remove threat actor access, patch vulnerability, clean systems). Recovery (restore systems, verify they are clean, monitor for re-infection). Post-Incident Activity (lessons learned, improve defenses, update playbooks). SIEM tools help detect. Chain of custody for evidence. Communication plan for stakeholders. Cyber insurance. Retainer with incident response firm."),
    ],
    "Project Manager": [
        ("What is the difference between Agile and Waterfall methodologies?",
         "Waterfall is sequential - each phase completes before next begins (Requirements, Design, Development, Testing, Deployment). Good for: well-defined requirements, regulatory projects, fixed-price contracts. Drawbacks: inflexible to change, testing late, customer sees product late. Agile is iterative and incremental - work in short sprints (2-4 weeks), deliver working software frequently, embrace change. Scrum and Kanban are Agile frameworks. Good for: changing requirements, innovative products, customer collaboration. Drawbacks: requires active customer involvement, harder to estimate cost/time upfront. Most projects today use Agile or hybrid approaches. Choose based on project type, requirements stability, customer availability."),
        ("Explain the Scrum framework.",
         "Scrum is Agile framework for developing products in iterative cycles called Sprints. Roles: Product Owner (prioritizes backlog, represents stakeholders), Scrum Master (facilitates process, removes impediments, servant leader), Development Team (self-organizing, cross-functional). Artifacts: Product Backlog (prioritized list of all features), Sprint Backlog (items for current sprint), Increment (potentially releasable product). Events: Sprint Planning (plan sprint work), Daily Scrum/Stand-up (15-min sync, what did I do, what will I do, any blockers), Sprint Review (demonstrate to stakeholders), Sprint Retrospective (how to improve process). Sprints are fixed time-boxes, typically 2 weeks."),
        ("How do you manage project risks?",
         "Risk management process: Identify risks (brainstorming, lessons learned, expert judgment, risk register). Analyze risks - Qualitative (probability and impact matrix - High/Medium/Low) and Quantitative (numerical probability, expected monetary value). Plan responses: Avoid (change plan to eliminate), Mitigate (reduce probability or impact), Transfer (insurance, contracts), Accept (acknowledge and monitor). Implement responses. Monitor and control (track identified risks, identify new risks). Risk register documents all risks, owners, responses, status. Risk appetite varies by organization. Early risk identification is cheapest. Top risks reviewed in every project status meeting. Learn from past project post-mortems."),
        ("What are KPIs and how do you measure project success?",
         "KPIs (Key Performance Indicators) measure project health and success. Common KPIs: Schedule Performance Index (SPI = EV/PV, >1 ahead of schedule), Cost Performance Index (CPI = EV/AC, >1 under budget), Schedule Variance (SV = EV-PV), Cost Variance (CV = EV-AC). Earned Value Management uses these. Other metrics: Defect density, team velocity (story points per sprint), customer satisfaction score, on-time delivery rate, scope creep percentage. Define success criteria upfront with stakeholders. Measure beyond iron triangle (scope/cost/schedule) to include quality, customer satisfaction, business value delivered. Regular reporting and dashboards for transparency."),
        ("How do you handle scope creep?",
         "Scope creep is uncontrolled changes or growth in project scope. Prevention: clear requirements documentation and sign-off, robust change control process, detailed project charter, work breakdown structure. When change requested: evaluate impact on timeline, budget, and resources. Use Change Request form. Present options to stakeholders with trade-offs. Prioritize against existing backlog. Get formal approval before implementing. Communicate impacts clearly. Common causes: vague initial requirements, stakeholder not involved early, poor documentation, trying to please everyone. Solution: Say 'yes and' not just 'no' - yes we can do that and here's what it costs/replaces. Build contingency buffer for inevitable changes."),
        ("Explain stakeholder management.",
         "Stakeholders are individuals or groups affected by the project. Management process: Identify all stakeholders (sponsor, customers, team, vendors, regulators). Analyze power and interest (power-interest grid): High power/high interest (manage closely), High power/low interest (keep satisfied), Low power/high interest (keep informed), Low power/low interest (monitor). Plan engagement strategy for each group. Engage stakeholders early and often. Regular status communications tailored to audience (executives want summary, technical team wants details). Manage expectations proactively. Address concerns before they become issues. Stakeholder register documents all stakeholders, their interests, and engagement strategy."),
        ("What is a project charter and what does it include?",
         "Project charter formally authorizes a project and documents initial requirements. It is the first document created and signed by project sponsor. Contents: Project title and description, business case and objectives, scope (what's included and excluded), deliverables, high-level milestones and timeline, budget estimate, key stakeholders, project manager authority level, key assumptions and constraints, high-level risks, success criteria. Charter gives project manager authority to use resources. It is not detailed - that comes in project plan. Without charter, projects lack formal authorization. Charter must be approved before planning begins. Keep it concise - 1-2 pages."),
        ("How do you create and manage a project schedule?",
         "Schedule creation process: Define activities (work breakdown structure - WBS breaks deliverables into manageable tasks). Sequence activities (identify dependencies - FS, SS, FF, SF). Estimate durations (expert judgment, analogous, parametric, three-point estimation). Develop schedule (critical path method identifies longest path, Gantt chart visualizes). Optimize (compress schedule via fast tracking - parallelize activities, or crashing - add resources). Baseline schedule for comparison. Manage schedule: weekly status updates, identify variance early (SPI metric), reforecast regularly. Tools: MS Project, Jira, Asana, Smartsheet. Buffer for risks. Communicate delays proactively with mitigation plan."),
        ("What is the critical path method?",
         "Critical Path Method (CPM) identifies longest sequence of dependent activities determining minimum project duration. Activities on critical path have zero float (delay) - any delay delays entire project. Steps: List all activities, identify dependencies, estimate durations, draw network diagram (AON - Activity on Node), calculate Early Start/Early Finish (forward pass), Late Start/Late Finish (backward pass), calculate float (LS-ES or LF-EF), critical path has zero float. Near-critical paths with minimal float also need attention. Focus PM attention on critical and near-critical activities. Crashing (adding resources) and fast-tracking (parallelizing) compress critical path. Resource leveling may change critical path."),
        ("How do you conduct effective project meetings?",
         "Meeting best practices: Set clear objective - if no decision needed, can it be an email? Invite only necessary people. Send agenda in advance with time allocations. Start and end on time. Assign facilitator and note-taker. Park off-topic items (parking lot). Ensure equal participation, manage dominant voices. Summarize decisions and action items at end. Send meeting minutes within 24 hours with decisions, action items, owners, due dates. Follow up on action items in next meeting. Types of project meetings: Daily stand-up (15 min, status), Weekly status (30-60 min, issues), Steering committee (monthly, executive), Lessons learned (end of project). Virtual meetings need extra engagement techniques."),
        ("What tools do you use for project management?",
         "Project management tools serve different purposes: Planning and scheduling: MS Project (comprehensive, complex), Jira (Agile, software teams), Asana, Monday.com (visual, flexible), Smartsheet (spreadsheet-like). Communication: Slack, Microsoft Teams, Zoom. Document management: Confluence, SharePoint, Google Drive. Risk management: risk register in Excel or integrated in PM tools. Reporting: Power BI, Tableau for dashboards, or built-in tool reporting. Budget tracking: Excel, ERP systems. Resource management: Resource Guru, Float. Choose tools based on team size, methodology, integration needs. Avoid tool overload. Key is adoption - best tool is one team actually uses. Consolidate where possible."),
        ("What is earned value management (EVM)?",
         "EVM is project performance measurement technique integrating scope, schedule, and cost. Key values: Planned Value (PV) - budgeted cost for planned work. Earned Value (EV) - budgeted cost for completed work. Actual Cost (AC) - actual cost of completed work. Variances: Cost Variance (CV = EV-AC, positive = under budget), Schedule Variance (SV = EV-PV, positive = ahead of schedule). Performance Indices: CPI = EV/AC (>1 good), SPI = EV/PV (>1 good). Forecasting: Estimate at Completion (EAC = BAC/CPI). EVM requires detailed work breakdown structure and time-phased budget. Provides objective measurement vs subjective 'percent complete'. Used on large government and construction projects."),
    ],
    "Cloud Engineer": [
        ("What is the difference between IaaS, PaaS, and SaaS?", "IaaS (Infrastructure as a Service) provides virtualized computing resources like VMs, storage, and networking — AWS EC2, Azure VMs. You manage OS and above. PaaS (Platform as a Service) provides a managed platform to build applications — AWS Elastic Beanstalk, Google App Engine. You manage only your application code. SaaS (Software as a Service) delivers complete applications over the internet — Gmail, Salesforce. You manage nothing infrastructure-related. IaaS gives most control, SaaS gives least. Choose based on how much control vs convenience you need."),
        ("Explain AWS core services.", "AWS core services include EC2 (virtual machines), S3 (object storage), RDS (managed relational databases), VPC (virtual private cloud networking), IAM (identity and access management), Lambda (serverless functions), CloudFront (CDN), Route 53 (DNS), ELB (load balancing), and CloudWatch (monitoring). EC2 is the compute backbone, S3 is the storage backbone. IAM controls all permissions. VPC provides network isolation. Lambda enables serverless architecture without managing servers. These services form the foundation of most AWS architectures."),
        ("What is auto-scaling and how does it work?", "Auto-scaling automatically adjusts the number of compute resources based on demand. AWS Auto Scaling monitors metrics like CPU utilization or request count. When a threshold is breached, it launches new instances (scale out) or terminates instances (scale in). Components: Launch Template defines instance configuration, Auto Scaling Group defines min/max/desired capacity, Scaling Policies define when to scale. Target tracking maintains a metric at a target value. Step scaling responds to specific threshold breaches. Scheduled scaling handles predictable load patterns. Auto-scaling reduces cost by running only what you need and improves availability."),
        ("What is a VPC and how do you design one?", "A VPC (Virtual Private Cloud) is a logically isolated network in the cloud where you launch your resources. Key components: Subnets divide VPC into smaller networks — public subnets have internet access, private subnets do not. Internet Gateway enables internet access for public subnets. NAT Gateway allows private subnet instances to access internet without being directly exposed. Route Tables control traffic routing. Security Groups act as virtual firewalls at instance level. NACLs provide subnet-level security. A typical design: public subnets for load balancers, private subnets for application servers, private subnets for databases. Multiple AZs for high availability."),
        ("What is serverless computing?", "Serverless computing lets you run code without provisioning or managing servers. AWS Lambda is the main serverless compute service. You write functions that are triggered by events — HTTP requests, S3 uploads, database changes, scheduled events. AWS manages scaling, availability, and infrastructure. You pay only for compute time consumed, not idle time. Benefits: no server management, automatic scaling, cost-effective for variable workloads, faster deployment. Limitations: cold starts add latency, maximum execution time limits, limited local state, debugging is harder. Best for event-driven applications, APIs, data processing, and scheduled tasks."),
        ("Explain cloud security best practices.", "Cloud security best practices: Principle of least privilege — IAM roles and policies grant minimum permissions needed. Enable MFA for all accounts, especially root. Encrypt data at rest (S3 SSE, EBS encryption) and in transit (TLS). Use VPC to isolate resources, never expose databases to internet. Enable CloudTrail for audit logging of all API calls. Use Security Groups and NACLs to control network access. Regularly rotate access keys and credentials. Enable GuardDuty for threat detection. Use AWS Config to track configuration changes. Implement WAF for web application protection. Conduct regular security assessments. Defense in depth: multiple security layers."),
        ("What is cloud cost optimization?", "Cloud cost optimization reduces spending without sacrificing performance. Key strategies: Right-sizing — match instance types to actual workload requirements, avoid over-provisioning. Reserved Instances or Savings Plans — commit to 1-3 year usage for 30-70% discount. Spot Instances for fault-tolerant workloads — up to 90% cheaper. Auto-scaling to run only what you need. S3 lifecycle policies to move data to cheaper storage tiers. Delete unused resources — snapshots, unattached EBS volumes, idle load balancers. Use Cost Explorer and Budgets for visibility. Tag resources for cost allocation. Architect for cloud-native patterns rather than lifting-and-shifting on-premises designs."),
        ("What is Terraform and how does it work?", "Terraform is an open-source Infrastructure as Code tool by HashiCorp. It uses declarative HCL (HashiCorp Configuration Language) to define infrastructure. You write configuration files describing desired infrastructure state. Terraform creates an execution plan showing what will change. Apply executes the plan and provisions resources. State file tracks current infrastructure state. Key concepts: Providers (AWS, Azure, GCP plugins), Resources (infrastructure components), Modules (reusable configurations), Variables (parameterize configurations), Outputs (expose values). Terraform is cloud-agnostic — same workflow for any provider. Remote state in S3 + state locking with DynamoDB for team collaboration."),
        ("What is the difference between containers and virtual machines?", "Virtual Machines (VMs) virtualize hardware — each VM has its own OS kernel, requires gigabytes of storage, takes minutes to boot. Strong isolation but resource-heavy. Containers virtualize at OS level — share the host kernel, require megabytes, start in seconds. Lightweight but share kernel so less isolation. Docker is the standard container runtime. VMs suit workloads needing full OS isolation, different OS types, or strong security boundaries. Containers suit microservices, consistent deployment environments, and high-density deployments. Kubernetes orchestrates containers at scale. In practice: VMs are the cloud instances, containers run inside VMs."),
        ("What is a CDN and when do you use it?", "A CDN (Content Delivery Network) distributes content across geographically dispersed servers (edge locations) closer to users. When a user requests content, it's served from the nearest edge location rather than the origin server. Benefits: reduced latency, decreased origin server load, better availability, DDoS protection. AWS CloudFront, Cloudflare, Azure CDN are popular choices. Use CDN for: static assets (images, CSS, JS), video streaming, large file downloads, websites with global audiences. Configure cache TTLs appropriately. Invalidate cache when content changes. Origin can be S3, EC2, or any HTTP server. CDN is essential for performance at global scale."),
        ("Explain disaster recovery strategies in cloud.", "Cloud DR strategies in order of cost and complexity: Backup and Restore — simplest, backup data to S3, restore when needed, hours of downtime acceptable. Pilot Light — minimal core infrastructure always running, scale up during disaster, minutes of downtime. Warm Standby — scaled-down fully functional environment always running, quickly scale to full production. Multi-Site Active-Active — full production running in multiple regions simultaneously, near-zero downtime. RTO (Recovery Time Objective) is acceptable downtime. RPO (Recovery Point Objective) is acceptable data loss. Higher availability requires more cost. Use Route 53 for DNS failover. Automate recovery procedures. Test DR regularly."),
        ("What is Kubernetes and how does it differ from Docker?", "Docker is a containerization platform for building and running individual containers. Kubernetes (K8s) is a container orchestration system for managing containers at scale across multiple hosts. Docker answers 'how to package and run one container'. Kubernetes answers 'how to manage hundreds of containers across a cluster'. Kubernetes provides: automatic deployment and scaling, self-healing (restart failed containers), load balancing, rolling updates, service discovery, config management. Key objects: Pod (smallest unit, one or more containers), Deployment (manages replica sets), Service (stable network endpoint), Ingress (HTTP routing). Docker Desktop and minikube for local development. EKS, GKE, AKS are managed Kubernetes services."),
        ("What is cloud monitoring and observability?", "Cloud monitoring tracks metrics, logs, and traces to understand system health and behavior. Three pillars of observability: Metrics (numerical measurements over time — CPU, memory, request rate, error rate), Logs (timestamped records of events), Traces (request flow across distributed services). AWS CloudWatch collects metrics and logs from AWS services. CloudWatch Alarms notify when thresholds breached. X-Ray provides distributed tracing. Third-party tools: Datadog, New Relic, Grafana with Prometheus. Key metrics: availability, latency, throughput, error rate (RED method). Resource metrics: CPU, memory, disk, network (USE method). Set up dashboards for operational visibility and alerts for anomaly detection."),
    ],
    "Database Administrator": [
        ("What is the difference between OLTP and OLAP?", "OLTP (Online Transaction Processing) handles day-to-day transactions — inserts, updates, deletes on operational data. Optimized for many small, fast queries. Normalized schema reduces redundancy. Examples: banking systems, e-commerce orders. OLAP (Online Analytical Processing) handles complex analytical queries over large datasets — aggregations, trends, reports. Optimized for read-heavy workloads over large data volumes. Denormalized schema (star/snowflake) improves query performance. Examples: data warehouses, business intelligence. OLTP uses row-based storage, OLAP uses columnar storage. Separate systems to avoid analytical queries impacting operational performance. ETL processes move data from OLTP to OLAP."),
        ("Explain database normalization and its forms.", "Normalization organizes data to reduce redundancy and improve integrity. First Normal Form (1NF): eliminate repeating groups, each column atomic, each row unique. Second Normal Form (2NF): must be in 1NF, no partial dependencies — all non-key columns depend on entire primary key. Third Normal Form (3NF): must be in 2NF, no transitive dependencies — non-key columns depend only on primary key. Boyce-Codd Normal Form (BCNF): stronger version of 3NF. Benefits: eliminates data anomalies (insert, update, delete), reduces storage. Downside: more joins needed for queries. In practice, normalize to 3NF then denormalize selectively for performance. Data warehouses often use denormalized star schemas."),
        ("What are database indexes and how do they work?", "Indexes are data structures that allow faster data retrieval at the cost of additional storage and slower writes. B-tree index is the default — balanced tree structure, efficient for range queries and equality checks, O(log n) lookup. Hash index is faster for equality lookups but cannot handle ranges. Composite index covers multiple columns. Covering index includes all columns needed by a query avoiding table access. Clustered index determines physical row order (primary key in InnoDB). Non-clustered index is separate structure pointing to rows. Create indexes on WHERE, JOIN, ORDER BY, GROUP BY columns. Too many indexes hurt INSERT/UPDATE/DELETE performance. Use EXPLAIN to analyze query plans and identify missing indexes."),
        ("What is a database transaction and ACID properties?", "A transaction is a sequence of operations executed as a single unit. ACID ensures reliability: Atomicity — all operations succeed or all are rolled back, no partial transactions. Consistency — database moves from one valid state to another, constraints always maintained. Isolation — concurrent transactions execute as if sequential, prevents dirty reads, phantom reads. Durability — committed transactions survive system failures, written to persistent storage. Isolation levels balance performance vs consistency: Read Uncommitted, Read Committed, Repeatable Read, Serializable. Use transactions for multi-step operations that must succeed together (bank transfer: debit + credit). BEGIN/COMMIT/ROLLBACK commands control transactions."),
        ("What is database replication?", "Replication copies data from one database server (primary/master) to one or more servers (replicas/slaves). Types: Synchronous replication — primary waits for replica to confirm write before acknowledging client, strong consistency but higher latency. Asynchronous replication — primary proceeds without waiting, better performance but replica may lag. Uses: High availability — failover to replica if primary fails. Read scaling — distribute read queries across replicas. Geographic distribution — replicas in different regions reduce latency. Backup — replicas provide live backup. MySQL supports statement-based and row-based replication. PostgreSQL uses streaming replication. Replication lag must be monitored. Read-your-writes consistency issues can occur with async replication."),
        ("Explain query optimization techniques.", "Query optimization improves slow query performance. Steps: Use EXPLAIN/EXPLAIN ANALYZE to view query execution plan — look for full table scans, missing indexes, inefficient joins. Add appropriate indexes based on query patterns. Rewrite queries: avoid SELECT *, use specific columns. Avoid functions on indexed columns in WHERE clauses. Use JOINs instead of subqueries where possible. Batch large DELETE/UPDATE operations. Use pagination (LIMIT/OFFSET or keyset pagination). Avoid N+1 query problems. Optimize schema: appropriate data types (INT not VARCHAR for numbers), proper column sizes. Analyze with slow query log to identify problematic queries. Use connection pooling. Cache frequently accessed read-only data in Redis. Partition large tables."),
        ("What is database sharding?", "Sharding horizontally partitions data across multiple database servers (shards), each holding a subset of data. Unlike replication (same data on multiple servers), sharding splits data so each server handles a portion of the total dataset. Shard key determines which shard stores each record. Sharding strategies: Range-based (by date or ID range), Hash-based (hash of shard key), Directory-based (lookup table). Benefits: horizontal scalability beyond single server limits, improved performance by distributing load. Challenges: cross-shard queries are complex, transactions spanning shards are difficult, rebalancing shards when adding servers. Used by large-scale systems like social networks. MongoDB, Cassandra have built-in sharding. Manual sharding at application layer is complex but gives more control."),
        ("What is the difference between SQL and NoSQL databases?", "SQL databases are relational — structured tables with predefined schema, ACID transactions, SQL query language. Good for: complex queries with joins, strong consistency requirements, structured data with clear relationships. Examples: PostgreSQL, MySQL, Oracle. NoSQL databases are non-relational — flexible schema, horizontal scaling, various data models. Document stores (MongoDB) store JSON-like documents. Key-value stores (Redis) are fastest for simple lookups. Column-family stores (Cassandra) excel at time-series and wide-column data. Graph databases (Neo4j) for highly connected data. NoSQL trades some ACID guarantees for scale and flexibility. Choose SQL for complex queries and transactions, NoSQL for high throughput, flexible schema, or specific data patterns."),
        ("What is database backup and recovery?", "Database backup strategy: Full backup — complete copy of all data, most storage, slowest. Incremental backup — only changes since last backup, fast but complex recovery. Differential backup — changes since last full backup, middle ground. Point-in-time recovery (PITR) uses full backup plus transaction logs to restore to any moment. Recovery objectives: RTO (Recovery Time Objective) — acceptable downtime. RPO (Recovery Point Objective) — acceptable data loss. Test backups regularly by restoring to verify integrity. Store backups off-site or in different cloud region. Automate backup schedules. MySQL: mysqldump for logical backups, xtrabackup for hot backups. PostgreSQL: pg_dump, pg_basebackup, WAL archiving. Cloud databases have automated backup features — use them."),
        ("What is connection pooling in databases?", "Connection pooling maintains a pool of pre-established database connections that applications reuse rather than creating new connections for each request. Creating a database connection is expensive — involves network handshake, authentication, and resource allocation taking 20-100ms. Pool initializes connections at startup, applications borrow connections, execute queries, and return them to pool. Configuration: minimum pool size (always available connections), maximum pool size (prevents overwhelming database), connection timeout (wait time for available connection), idle timeout (close unused connections). Tools: PgBouncer for PostgreSQL, HikariCP for Java, SQLAlchemy pool for Python. Too small pool causes queuing and timeouts, too large overwhelms database. Monitor pool utilization to tune appropriately."),
        ("What is database partitioning?", "Partitioning divides a large table into smaller, manageable pieces while appearing as one table to applications. Unlike sharding, partitioning happens within a single database instance. Types: Range partitioning — by date ranges (monthly partitions for logs), good for time-series data. List partitioning — by specific values (region, status). Hash partitioning — by hash function for even distribution. Composite partitioning combines methods. Benefits: improved query performance (partition pruning scans only relevant partitions), easier data management (drop old partitions), better maintenance (vacuum/analyze partitions independently), parallel query execution. PostgreSQL uses declarative partitioning. MySQL uses PARTITION BY syntax. Good for large tables with clear partitioning key and range-based queries."),
        ("What are stored procedures and when to use them?", "Stored procedures are precompiled SQL code stored in the database that can be called with parameters. Benefits: reduced network traffic (multiple operations in one call), code reuse, security (grant execute without table access), encapsulate business logic, precompiled for performance. When to use: complex multi-step operations, enforce business rules at database level, batch operations, when network round trips are bottleneck. Drawbacks: harder to version control, difficult to test, tight coupling of business logic to database, harder to debug, not portable across databases. Modern ORMs and microservices often avoid stored procedures for portability. Use for performance-critical database-heavy operations where the tradeoffs are justified."),
    ],
    "Machine Learning Engineer": [
        ("What is the difference between machine learning and deep learning?", "Machine learning (ML) is a subset of AI where algorithms learn patterns from data to make predictions or decisions without being explicitly programmed. Includes algorithms like linear regression, decision trees, SVM, random forests. Deep learning (DL) is a subset of ML using neural networks with many layers. Automatically learns feature representations from raw data. Excels at unstructured data — images, audio, text. ML requires feature engineering by domain experts, DL learns features automatically. ML works well with smaller datasets, DL needs large amounts of data and compute. ML models are more interpretable, deep learning is often a black box. Use classical ML for structured tabular data, deep learning for images, NLP, and speech."),
        ("What is the ML model development lifecycle?", "ML lifecycle stages: Problem Definition — frame business problem as ML task, define success metrics, identify data needs. Data Collection — gather from databases, APIs, web scraping. Exploratory Data Analysis (EDA) — understand distributions, correlations, outliers. Data Preprocessing — handle missing values, encode categoricals, scale features. Feature Engineering — create meaningful features from raw data. Model Selection — choose algorithm based on problem type and data. Training — fit model on training data with cross-validation. Evaluation — measure performance on held-out test set using appropriate metrics. Hyperparameter Tuning — optimize model parameters. Deployment — serve model via API or batch pipeline. Monitoring — track model performance drift over time. Retrain when performance degrades."),
        ("What is overfitting and how do you prevent it?", "Overfitting occurs when a model learns training data too well including noise, performing poorly on unseen data. Signs: high training accuracy, much lower validation accuracy. Prevention techniques: More training data — largest data volume reduces overfitting risk. Cross-validation — k-fold CV provides reliable performance estimate. Regularization — L1 (Lasso) promotes sparsity, L2 (Ridge) penalizes large weights. Dropout — randomly disable neurons during training in neural networks. Early stopping — stop training when validation loss stops improving. Reduce model complexity — fewer parameters, shallower networks. Data augmentation — artificially increase training data variety. Ensemble methods — combine multiple models. Feature selection — remove irrelevant features. Always evaluate on truly held-out test data."),
        ("Explain gradient descent and its variants.", "Gradient descent optimizes model parameters by iteratively moving in the direction of steepest loss reduction. Compute gradient of loss with respect to parameters, update parameters by learning_rate * gradient. Batch Gradient Descent uses entire dataset per update — stable but slow for large datasets. Stochastic Gradient Descent (SGD) updates per single sample — fast but noisy. Mini-batch Gradient Descent uses small batches — balances speed and stability, most commonly used. Variants: Momentum — accumulates velocity to accelerate convergence. RMSprop — adaptive learning rate per parameter. Adam — combines momentum and RMSprop, most popular optimizer. Learning rate is critical hyperparameter — too high causes divergence, too low is very slow. Learning rate schedules reduce rate during training."),
        ("What is model deployment and how do you serve ML models?", "Model deployment makes trained models available for inference in production. Deployment patterns: Real-time inference — REST API serving predictions on demand, Flask/FastAPI wrapping model, low latency required. Batch inference — process large datasets periodically, not latency-sensitive. Streaming inference — process data streams with Kafka/Spark. Tools: MLflow for model versioning and registry. Docker containers for consistent deployment. Kubernetes for scalable serving. TensorFlow Serving, TorchServe for framework-native serving. Cloud services: AWS SageMaker, Google AI Platform, Azure ML. Key considerations: model versioning for rollback, A/B testing new models, monitoring for prediction drift, handling model loading time, feature consistency between training and serving pipelines. CI/CD for ML models."),
        ("What is feature engineering?", "Feature engineering transforms raw data into features that better represent the underlying patterns for ML models. Techniques: Numerical transformations — log transform for skewed data, polynomial features for non-linear relationships, binning continuous variables. Categorical encoding — one-hot encoding for nominal categories, label encoding for ordinal, target encoding for high cardinality. Date/time features — extract day of week, hour, month, is_weekend, days since event. Text features — TF-IDF, word embeddings, bag of words. Interaction features — multiply or combine related features. Aggregation features — mean/count/sum of related records (customer's average purchase). Lag features for time series. Domain knowledge drives the most impactful features. Often more important than algorithm choice."),
        ("What is cross-validation and why is it important?", "Cross-validation evaluates model performance more reliably than a single train/test split. K-fold CV: split data into k equal folds, train on k-1 folds, test on remaining fold, repeat k times, average results. Stratified K-fold preserves class distribution in each fold for classification problems. Leave-one-out CV uses each sample once as test set — good for small datasets. Benefits: uses all data for both training and evaluation, more reliable performance estimate, detects overfitting. Helps compare models and tune hyperparameters without touching final test set. Nested cross-validation for unbiased hyperparameter tuning. Always hold out a final test set completely separate from all cross-validation. Prevents optimistic performance estimates from data leakage."),
        ("What is the difference between classification and regression?", "Classification predicts a discrete category label. Binary classification: two classes (spam/not spam, fraud/not fraud). Multi-class: multiple categories (image recognition, language detection). Algorithms: Logistic Regression, Decision Trees, SVM, Random Forest, Neural Networks. Metrics: accuracy, precision, recall, F1 score, AUC-ROC, confusion matrix. Regression predicts a continuous numerical value — house price, temperature, stock price. Algorithms: Linear Regression, Ridge/Lasso, Decision Trees, Random Forest, Neural Networks. Metrics: MAE, MSE, RMSE, R-squared. Some problems can be framed either way — predicting exact churn date (regression) vs will customer churn in 30 days (classification). Choose based on what output the business needs."),
        ("What is MLOps and why is it important?", "MLOps applies DevOps principles to machine learning to standardize and streamline ML lifecycle. Addresses challenges unique to ML: models degrade over time as data distribution shifts (model drift), experiments need tracking, datasets need versioning alongside code, reproducing results is hard. Key practices: Version control for code, data, and models. Experiment tracking with MLflow or Weights & Biases — log parameters, metrics, artifacts. CI/CD pipelines for automated model training, testing, and deployment. Model registry for managing model versions and deployment stages. Monitoring for data drift, prediction drift, model performance in production. Feature stores for consistent features between training and serving. Automated retraining pipelines when drift detected. MLOps reduces time from model development to production and improves reliability."),
        ("Explain ensemble methods in machine learning.", "Ensemble methods combine multiple models to produce better predictions than any single model. Bagging (Bootstrap Aggregating): trains multiple models independently on random data subsets, averages predictions. Reduces variance. Random Forest is bagging with decision trees plus random feature selection at each split. Boosting: trains models sequentially where each focuses on previous model's errors. Reduces bias and variance. AdaBoost re-weights misclassified samples. Gradient Boosting builds trees to minimize loss gradient. XGBoost and LightGBM are highly optimized gradient boosting implementations — often best for tabular data competitions. Stacking: trains a meta-model on predictions of base models. Voting: majority vote for classification, average for regression. Ensemble methods improve accuracy but increase computational cost and reduce interpretability."),
        ("What is natural language processing (NLP)?", "NLP enables computers to understand, interpret, and generate human language. Key tasks: Text classification (sentiment analysis, spam detection), Named Entity Recognition (identify people, places, organizations), Machine Translation, Text Summarization, Question Answering, Text Generation. Pipeline: Tokenization (split text into tokens), Stop word removal, Stemming/Lemmatization, Feature extraction (TF-IDF, word embeddings). Word embeddings (Word2Vec, GloVe) represent words as dense vectors capturing semantic meaning. Transformers architecture revolutionized NLP — BERT, GPT, T5 achieve state-of-the-art. Hugging Face library provides pretrained transformer models. Transfer learning: fine-tune pretrained models on domain-specific data. spaCy and NLTK for classical NLP. Modern approach: use large language models with prompt engineering or fine-tuning."),
        ("What metrics do you use to evaluate ML models?", "Metric selection depends on problem type and business objective. Classification metrics: Accuracy (correct/total) — misleading for imbalanced classes. Precision (true positives / predicted positives) — important when false positives are costly. Recall/Sensitivity (true positives / actual positives) — important when false negatives are costly. F1 Score (harmonic mean of precision and recall) — balance for imbalanced datasets. AUC-ROC measures discrimination ability across thresholds. Log loss for probabilistic predictions. Regression metrics: MAE (Mean Absolute Error) — interpretable, robust to outliers. MSE/RMSE penalizes large errors more. R-squared measures variance explained. MAPE (Mean Absolute Percentage Error) for relative errors. Always align metrics with business cost of different error types. Monitor metrics in production, not just offline evaluation."),
    ],
    "Mobile Developer": [
        ("What is the difference between native, hybrid, and cross-platform mobile development?", "Native development builds separate apps for each platform using platform-specific languages — Swift/Objective-C for iOS, Kotlin/Java for Android. Best performance, full platform API access, best UX but requires separate codebases and teams. Hybrid apps use web technologies (HTML, CSS, JS) wrapped in a native container (Cordova, Ionic). Single codebase but performance limitations and look-and-feel compromises. Cross-platform frameworks (React Native, Flutter) use one codebase that compiles to native code. Flutter uses Dart and compiles to native ARM code. React Native uses JavaScript and bridges to native components. Good performance, single codebase, but some native APIs require plugins. Choose native for performance-critical apps, cross-platform for faster development with near-native performance."),
        ("Explain the Android activity lifecycle.", "Android Activity lifecycle manages how the OS handles activities as users navigate. States: onCreate() — activity created, initialize UI, called once per lifecycle. onStart() — activity visible but not interactive. onResume() — activity in foreground, fully interactive, restore resources. onPause() — activity partially obscured, save critical state, release camera/sensors. onStop() — activity no longer visible, release more resources. onRestart() — activity coming back from stopped state. onDestroy() — activity being destroyed, final cleanup. Important: always release expensive resources in onPause/onStop and restore in onResume/onStart. The OS can destroy stopped activities to reclaim memory. Use ViewModel to survive configuration changes like screen rotation. Understand the lifecycle to prevent memory leaks and data loss."),
        ("What is React Native and how does it work?", "React Native is Facebook's cross-platform mobile framework using JavaScript and React. Write components in JavaScript/TypeScript, React Native bridges to native UI components. Unlike hybrid apps, renders actual native components (not WebView), giving native look and feel. Architecture: JS thread runs business logic, Native thread renders UI, Bridge communicates between them asynchronously. New Architecture uses JSI (JavaScript Interface) for synchronous native calls, replacing the old Bridge. Expo is a framework built on React Native providing managed workflow, easy setup, and OTA updates. Hot reloading speeds development. Access native APIs through community packages or custom native modules. Performance is close to native but heavy animations may need optimization. Large community and ecosystem."),
        ("What is Flutter and what makes it different?", "Flutter is Google's cross-platform framework using the Dart language. Unlike React Native which uses native components, Flutter renders everything using its own Skia/Impeller graphics engine — provides pixel-perfect consistency across platforms and OS versions. Single codebase targets iOS, Android, Web, Desktop, and embedded. Hot reload for fast development iteration. Dart compiles to native ARM code for performance. Widget-based architecture — everything is a widget. StatelessWidget for static UI, StatefulWidget for dynamic UI with setState. Provider, Riverpod, Bloc for state management. Flutter has its own Material and Cupertino widget libraries. Excellent performance, consistent UI, but larger app size and Dart learning curve. Growing rapidly in adoption."),
        ("What is state management in mobile apps?", "State management handles how application data flows and updates UI. Local state in a single widget is simple — setState() in Flutter, useState in React Native. As apps grow, state needs to be shared across multiple screens. Solutions for Flutter: Provider (simple, recommended for most apps), Riverpod (improved Provider), Bloc/Cubit (event-driven, good for complex logic), GetX (all-in-one), MobX (reactive). Solutions for React Native: Context API (built-in, simple sharing), Redux (predictable, verbose), Zustand (simple), MobX, Recoil. Key principles: keep state as local as possible, lift state when needed, separate business logic from UI, make state changes predictable. Global state should be used sparingly — only for truly shared data like auth status, theme."),
        ("How do you handle offline functionality in mobile apps?", "Offline functionality allows apps to work without internet connectivity. Strategies: Local database storage — SQLite (via sqflite in Flutter, react-native-sqlite-storage), Room (Android), CoreData (iOS) for structured data. Key-value storage — SharedPreferences/NSUserDefaults for simple settings. File storage for documents and media. Sync strategy: queue operations when offline, sync when connectivity restored. Conflict resolution when same data modified offline on multiple devices. Change tracking — track what changed and when. Libraries: Hive for Flutter (fast NoSQL), WatermelonDB for React Native. Connectivity monitoring with connectivity_plus or NetInfo packages. Show appropriate UI for offline state. Cache API responses with expiration. Background sync when app regains connectivity. Test offline scenarios explicitly."),
        ("What are push notifications and how do you implement them?", "Push notifications allow servers to send messages to devices even when app is not running. Architecture: App registers with platform notification service (APNs for iOS, FCM for Android), receives device token, sends token to your server. Server sends notification to FCM/APNs with device token, platform delivers to device. Firebase Cloud Messaging (FCM) works for both Android and iOS. Implementation: integrate firebase_messaging (Flutter) or @react-native-firebase/messaging (React Native). Request notification permissions (mandatory on iOS, Android 13+). Handle token refresh. Process notifications in foreground, background, and terminated states. Local notifications for scheduled reminders don't require server. Deep linking from notification to specific app screen. A/B test notification content and timing. Respect user preferences — allow granular notification settings."),
        ("What is the difference between iOS and Android development?", "iOS development uses Swift (modern) or Objective-C, Xcode IDE, targets iPhones and iPads. App distribution only through Apple App Store — requires paid developer account, strict review process. Human Interface Guidelines define design patterns. UIKit (programmatic/storyboard) or SwiftUI (declarative). Android development uses Kotlin (modern) or Java, Android Studio IDE, diverse device ecosystem. Distribution through Google Play Store (less strict review) and side-loading. Material Design guidelines. XML layouts or Jetpack Compose (declarative). Key differences: iOS has more consistent hardware, Android has extreme device fragmentation. iOS users tend to have higher app store spending. iOS requires Mac for development. Android apps need testing on more devices. Both support similar patterns but different APIs — camera, sensors, background processing differ significantly."),
        ("How do you optimize mobile app performance?", "Mobile performance optimization covers multiple areas. Rendering performance: avoid unnecessary widget rebuilds in Flutter (const constructors, selective rebuilds), use React Native's memo and useCallback to prevent re-renders. Use ListView.builder/FlatList for long lists (render only visible items). Image optimization: compress images, use appropriate formats (WebP), cache network images. Network optimization: minimize API calls, implement caching, paginate large data, compress requests. Startup performance: lazy load non-critical code, defer initialization, use splash screens. Memory management: dispose controllers and animations, use weak references, profile with DevTools. Battery: batch network requests, avoid wake locks, use background processing judiciously. Profile before optimizing — use Flutter DevTools or Android Profiler to identify actual bottlenecks."),
        ("What is mobile app security best practices?", "Mobile security best practices: Secure data storage — never store sensitive data (passwords, tokens) in plain text. Use Keychain (iOS) or Keystore (Android) for secrets. Encrypt local database. Network security — use HTTPS only, implement certificate pinning to prevent MITM attacks, validate SSL certificates. Authentication — implement biometric authentication, use secure token storage, short token expiry with refresh tokens. Input validation — validate all inputs, sanitize data, prevent injection attacks. Code obfuscation — enable ProGuard (Android) and Swift optimizations to make reverse engineering harder. Prevent screenshot of sensitive screens. Implement root/jailbreak detection for sensitive apps. Regular dependency updates to patch vulnerabilities. Don't log sensitive data. Follow OWASP Mobile Top 10 guidelines."),
        ("What is deep linking in mobile apps?", "Deep linking allows external URLs to open specific content within a mobile app. Types: Traditional deep links (myapp://product/123) — only work if app is installed. Universal Links (iOS) and App Links (Android) — HTTPS URLs that open app if installed, fall back to website if not. Deferred deep links — work even if app not yet installed, redirect to app store then to content after install. Implementation: configure URL schemes in iOS Info.plist and Android AndroidManifest.xml. Set up associated domains for Universal Links (apple-app-site-association file on server). Handle incoming links in app and navigate to appropriate screen. Use Branch.io or Firebase Dynamic Links for advanced deep linking with analytics. Deep links are essential for marketing campaigns, email links, social sharing, and cross-app navigation."),
    ],
    "Computer Networks": [
        ("What is the OSI model?", "The OSI (Open Systems Interconnection) model has 7 layers describing network communication. Layer 1 Physical — cables, wireless signals, bits. Layer 2 Data Link — MAC addresses, Ethernet, switches, frames. Layer 3 Network — IP addresses, routing, packets — routers operate here. Layer 4 Transport — TCP/UDP, ports, end-to-end communication, segments. Layer 5 Session — establishing and managing sessions. Layer 6 Presentation — data encoding, encryption, compression. Layer 7 Application — HTTP, FTP, DNS, SMTP — user-facing protocols. Mnemonic: Please Do Not Throw Sausage Pizza Away. Troubleshoot bottom-up: check physical, then data link, then IP. TCP/IP model simplifies to 4 layers: Network Access, Internet, Transport, Application. Understanding OSI helps diagnose where in the stack a problem occurs."),
        ("What is the difference between TCP and UDP?", "TCP (Transmission Control Protocol) is connection-oriented — establishes connection via 3-way handshake (SYN, SYN-ACK, ACK) before data transfer. Guarantees delivery with acknowledgments, retransmits lost packets. Flow control prevents sender overwhelming receiver. Congestion control reduces rate when network is congested. Ordered delivery — data arrives in sequence. Use TCP for: HTTP, FTP, email, file transfer — anything requiring reliability. UDP (User Datagram Protocol) is connectionless — sends packets without establishing connection. No delivery guarantees, no ordering, no congestion control. Faster and lower overhead. Use UDP for: video streaming, online gaming, DNS, VoIP — applications tolerating some loss but needing low latency. QUIC protocol (HTTP/3) builds reliability on top of UDP with better performance than TCP."),
        ("What is subnetting and CIDR notation?", "Subnetting divides a large network into smaller subnetworks. IP address has network portion and host portion. Subnet mask defines the boundary — 255.255.255.0 means first 24 bits are network. CIDR (Classless Inter-Domain Routing) notation: 192.168.1.0/24 means 24 bits for network, 8 bits for hosts. /24 supports 254 hosts (256 - 2 for network and broadcast addresses). /25 splits /24 into two subnets of 126 hosts each. /32 is single host. Benefits: efficient IP allocation, improved security by isolating network segments, reduced broadcast domain size. Subnetting calculation: /24 = 256 addresses, /25 = 128, /26 = 64, /27 = 32. In cloud VPCs, subnetting separates public from private resources and provides network isolation."),
        ("How does DNS work?", "DNS (Domain Name System) translates human-readable domain names to IP addresses. Resolution process: Browser checks local cache, then OS cache, then recursive resolver (usually ISP or 8.8.8.8). Recursive resolver checks its cache, queries root nameserver which directs to TLD nameserver (.com, .org). TLD nameserver directs to authoritative nameserver for the domain. Authoritative nameserver returns IP address. Result cached with TTL. Record types: A record maps domain to IPv4 address. AAAA for IPv6. CNAME is alias pointing to another domain. MX for mail servers. TXT for SPF, DKIM email verification. NS records list authoritative nameservers. SOA for zone information. TTL (Time To Live) controls caching duration. Low TTL allows faster DNS changes but increases query volume."),
        ("What is HTTP and how does HTTPS differ?", "HTTP (HyperText Transfer Protocol) is the foundation of web communication. Request-response protocol: client sends request with method (GET, POST, PUT, DELETE), URL, headers, optional body. Server responds with status code, headers, and body. Stateless — each request is independent, sessions managed via cookies. HTTP/1.1 introduced persistent connections and pipelining. HTTP/2 added multiplexing (multiple requests over one connection), header compression, server push — significantly faster. HTTP/3 uses QUIC (UDP-based) for better performance especially on lossy connections. HTTPS = HTTP + TLS encryption. TLS provides: confidentiality (encrypt traffic), integrity (detect tampering), authentication (verify server identity via certificates). HSTS forces HTTPS. Certificate Transparency logs prevent fraudulent certificates. All production web traffic should use HTTPS."),
        ("What is a firewall and how does it work?", "A firewall controls network traffic based on security rules, acting as a barrier between trusted and untrusted networks. Types: Packet filter firewall inspects individual packets based on source/destination IP, port, and protocol. Fast but no application awareness. Stateful firewall tracks connection state, understands context. Knows if a packet is part of established connection or new/unsolicited. Application layer firewall (Layer 7) inspects application content — detects SQL injection, XSS in HTTP traffic. Next-generation firewall (NGFW) combines stateful inspection, application awareness, IPS, and SSL inspection. Web Application Firewall (WAF) specifically protects web applications. Host-based firewalls on individual servers (iptables on Linux, Windows Firewall). Network firewalls at perimeter. Defense in depth uses both. Rules are typically allow-list based — deny by default, explicitly permit needed traffic."),
        ("What is the difference between a hub, switch, and router?", "Hub is Layer 1 device that broadcasts all traffic to all ports — no intelligence, creates collision domain, obsolete. Switch is Layer 2 device that learns MAC addresses and forwards frames only to correct port. Each port is a separate collision domain. Switches create separate collision domains but share broadcast domain. VLANs on switches create separate broadcast domains. Managed switches support VLANs, QoS, spanning tree. Router is Layer 3 device that routes packets between different networks using IP addresses. Routes between subnets and networks. Contains routing table. Connects different network segments and provides network isolation. NAT on routers translates private to public IP. Default gateway for local hosts. Modern home devices combine switch and router functionality. Data center uses dedicated switches (Layer 2 and Layer 3) and routers for different functions."),
        ("What is NAT and why is it used?", "NAT (Network Address Translation) modifies IP address information in packet headers as they pass through a router. Most commonly used to allow multiple private IP addresses (192.168.x.x, 10.x.x.x) to share a single public IP address. How it works: router maintains NAT table mapping private IP:port to public IP:port. Outbound packets have source address replaced with public IP. Inbound responses are translated back to private IP. Types: Static NAT — one-to-one mapping of private to public IP. Dynamic NAT — pool of public IPs shared. PAT/Masquerade (most common) — many private IPs share one public IP using different ports. Benefits: IPv4 address conservation, security (private hosts not directly addressable from internet). Drawbacks: breaks end-to-end connectivity, complicates hosting services (requires port forwarding), problematic for some protocols."),
        ("What is VPN and how does it work?", "A VPN (Virtual Private Network) creates an encrypted tunnel over a public network providing secure communication as if on a private network. Working: VPN client and server authenticate each other, negotiate encryption. Client sends traffic to VPN server which forwards to destination, appearing to come from server's IP. Return traffic goes back through tunnel. All traffic between client and server is encrypted. Protocols: OpenVPN (open-source, flexible), WireGuard (modern, fast, simple), IPsec (widely supported, used for site-to-site), L2TP/IPsec, PPTP (obsolete, insecure). Use cases: Remote access — employees securely connecting to corporate network. Site-to-site — connecting branch offices. Privacy — hiding traffic from ISP and changing apparent location. Bypassing geo-restrictions. Split tunneling sends only corporate traffic through VPN while personal traffic goes direct."),
        ("What is load balancing?", "Load balancing distributes incoming network traffic across multiple servers to ensure no single server is overwhelmed. Benefits: improved availability (if one server fails, others handle traffic), scalability (add servers to increase capacity), better performance (reduced server load means faster responses). Algorithms: Round Robin — requests distributed sequentially. Least Connections — routes to server with fewest active connections. IP Hash — same client always goes to same server (session persistence). Weighted Round Robin — servers with more capacity get more requests. Types: Layer 4 (transport) load balancer routes based on IP and TCP/UDP ports, very fast. Layer 7 (application) load balancer routes based on HTTP headers, URL, cookies — supports content-based routing. Health checks detect failed servers and remove them from rotation. AWS ELB, Nginx, HAProxy are popular load balancers. Sticky sessions maintain user session on same server."),
        ("What is network latency and how do you reduce it?", "Network latency is the time delay for data to travel from source to destination. Measured in milliseconds (ms). Components: propagation delay (speed of light over distance), transmission delay (time to push bits onto wire), processing delay (routers examining packets), queuing delay (waiting in buffers). Causes of high latency: geographic distance, congested network paths, slow DNS resolution, too many network hops, server-side processing time. Reduction strategies: CDN to serve content from edge locations near users. Reduce DNS lookup time with fast DNS resolvers. HTTP/2 or HTTP/3 to reduce request overhead. Connection reuse (keep-alive). Minimize redirects. Compress data to reduce transmission time. Optimize server response time. Use TCP optimizations (TCP Fast Open). For real-time applications (gaming, video calls) use UDP-based protocols. Monitor with traceroute to identify slow hops."),
        ("What is network security and common attacks?", "Network security protects integrity, confidentiality, and availability of network resources. Common attacks: DDoS (Distributed Denial of Service) — overwhelm target with traffic from many sources. Mitigation: rate limiting, traffic scrubbing, CDN. Man-in-the-Middle (MITM) — attacker intercepts communication between two parties. Prevention: HTTPS, certificate pinning, HSTS. ARP Spoofing — attacker poisons ARP cache to intercept local network traffic. DNS Spoofing — malicious DNS responses redirect users to fake sites. Prevention: DNSSEC. Port Scanning — attackers discover open services. Defense: close unnecessary ports, IDS/IPS. Packet Sniffing — capturing network traffic. Prevention: encryption. Social engineering targets humans not technology. Defense layers: firewalls, IDS/IPS, VPN, network segmentation, encryption in transit, strong authentication, regular security audits, network monitoring."),
    ],
    "Software Architect": [
        ("What is software architecture and what does an architect do?", "Software architecture defines the high-level structure of a software system — major components, their relationships, and how they interact. An architect makes decisions that are hard to change later and have system-wide impact. Responsibilities: Define architectural patterns and styles (microservices, monolith, event-driven). Make technology stack decisions. Define quality attributes (performance, scalability, security, maintainability). Create architecture documentation. Guide development teams on implementation. Review code for architectural compliance. Identify and mitigate technical risks. Balance competing concerns — feature velocity vs technical debt, consistency vs autonomy. Architects must deeply understand the business domain, technical constraints, and team capabilities. Good architecture enables the team to move fast sustainably; bad architecture creates bottlenecks and fragility."),
        ("What is the difference between monolithic and microservices architecture?", "Monolithic architecture packages entire application as single deployable unit. All components share same process and database. Benefits: simple development and deployment, easy debugging, efficient inter-process communication, no distributed system complexity. Drawbacks: scaling entire app for one bottleneck, long build/deploy times, technology lock-in, team coordination overhead as codebase grows. Microservices architecture decomposes application into small independent services each with own process and database, communicating via APIs. Benefits: independent deployment and scaling, technology flexibility per service, team autonomy, fault isolation. Drawbacks: distributed system complexity, network latency, data consistency challenges, operational overhead (monitoring, service discovery, distributed tracing). Start with monolith, extract services when pain is felt. Many successful systems are modular monoliths — well-structured without microservices complexity."),
        ("What is domain-driven design (DDD)?", "DDD is an approach to software development focusing on modeling software around the business domain. Key concepts: Ubiquitous Language — common vocabulary shared by domain experts and developers, used in code and conversations. Bounded Context — explicit boundary within which a domain model applies, different contexts can have different models of same concept. Aggregate — cluster of domain objects treated as single unit with one root entity enforcing invariants. Entity — object with unique identity persisting over time. Value Object — immutable object defined by its attributes, no identity. Domain Events — record that something significant happened in the domain. Repository — abstraction for accessing aggregates. Application Service — orchestrates use cases. DDD is most valuable for complex domains with rich business logic, less useful for simple CRUD applications."),
        ("What are SOLID principles?", "SOLID is five object-oriented design principles for maintainable code. Single Responsibility Principle (SRP) — a class should have only one reason to change, one job. Open/Closed Principle (OCP) — classes should be open for extension but closed for modification — use inheritance or composition to add behavior. Liskov Substitution Principle (LSP) — subclasses should be substitutable for their base classes without changing program correctness. Interface Segregation Principle (ISP) — clients should not depend on interfaces they don't use — create specific smaller interfaces rather than one large general interface. Dependency Inversion Principle (DIP) — depend on abstractions not concretions — high-level modules should not depend on low-level modules, both should depend on abstractions. SOLID principles reduce coupling, increase cohesion, and make code easier to test and modify."),
        ("What is the CAP theorem?", "CAP theorem states that a distributed system can guarantee at most two of three properties simultaneously. Consistency (C) — all nodes see the same data at the same time, every read gets the most recent write. Availability (A) — every request receives a response (not necessarily the most recent data), system remains operational. Partition Tolerance (P) — system continues operating despite network partitions (lost messages between nodes). In practice, network partitions happen in distributed systems, so you must choose between CP and AP during partitions. CP systems (strong consistency) — when partition occurs, reject requests to maintain consistency. Examples: HBase, MongoDB (strong consistency mode), ZooKeeper. AP systems (high availability) — when partition occurs, respond with potentially stale data. Examples: Cassandra, CouchDB, DynamoDB. Most modern systems are tunable — allow configuring consistency level per operation."),
        ("What are common architectural patterns?", "Key architectural patterns: Layered (N-tier) — separates concerns into layers (presentation, business logic, data access), simple and well-understood. Event-Driven — components communicate through events, highly decoupled, good for async workflows. Microservices — small independent services, scalable but complex. Serverless — functions triggered by events, no server management, cost-effective for variable load. CQRS (Command Query Responsibility Segregation) — separate models for reads and writes, optimizes each independently. Event Sourcing — store events not current state, complete audit trail, rebuild state by replaying events. Hexagonal (Ports and Adapters) — isolates core business logic from external systems via ports and adapters. Strangler Fig — incrementally migrate from monolith by replacing functionality piece by piece. Choose pattern based on team size, scalability requirements, consistency needs, and team expertise."),
        ("What is system design? How do you design a scalable system?", "System design involves defining system architecture to meet functional and non-functional requirements. Process: Clarify requirements — functional (what it does) and non-functional (scale, latency, availability). Estimate scale — users, requests per second, data volume. Design high-level architecture — identify major components. Deep dive into critical components. Discuss tradeoffs. Scalability techniques: Horizontal scaling — add more servers, use load balancers. Caching — Redis/Memcached reduce database load, CDN for static content. Database scaling — read replicas for read-heavy loads, sharding for write scale, NoSQL for flexible schema at scale. Message queues — decouple components, handle traffic spikes, async processing. Microservices — scale individual services independently. CDN for static and edge content. Data partitioning strategies. Design for failure — redundancy, circuit breakers, graceful degradation."),
        ("What is event-driven architecture?", "Event-driven architecture (EDA) uses events to communicate between loosely coupled components. An event represents something that happened — OrderPlaced, UserRegistered, PaymentProcessed. Producers emit events without knowing who consumes them. Consumers subscribe to events they care about. Message brokers (Kafka, RabbitMQ, AWS SNS/SQS) transport events. Benefits: loose coupling between services, easy to add new consumers without modifying producer, natural audit trail, handles traffic spikes by queuing. Patterns: Event notification (fire and forget), Event-carried state transfer (event contains full data), Event sourcing (events as source of truth). Challenges: eventual consistency, debugging event flows is complex, duplicate event handling, ordering guarantees. Combine with CQRS — commands trigger events, events update read models. Essential for microservices communication and real-time data pipelines."),
        ("What is API design best practices?", "Good API design makes integration easy and reduces errors. RESTful principles: use nouns for resources (not verbs), HTTP methods semantically (GET read, POST create, PUT/PATCH update, DELETE remove), meaningful status codes (200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 429 Rate Limited, 500 Server Error). Versioning strategy: URL versioning (/api/v1/), header versioning. Consistent response format including data, errors, pagination metadata. Pagination for list endpoints (cursor-based scales better than offset). Filter, sort, and search via query parameters. HATEOAS links for discoverability. Error responses with actionable messages (what went wrong, how to fix). Rate limiting to prevent abuse. Authentication via Bearer tokens. Documentation with OpenAPI/Swagger. Idempotent operations for retries. GraphQL as alternative for flexible queries."),
        ("What is technical debt and how do you manage it?", "Technical debt is the implied cost of rework caused by choosing expedient solutions now instead of better approaches that would take longer. Types: Deliberate debt — conscious shortcuts to meet deadlines. Accidental debt — poor design from inexperience. Bit rot — once-good code becoming outdated. Consequences: slows development velocity, increases bug rate, makes onboarding harder, can become unmanageable. Management strategies: Track debt explicitly in backlog with estimated cost. Allocate regular capacity (20% sprint capacity) for debt reduction. Boy Scout Rule — leave code better than you found it. Prioritize debt that causes most pain — on critical paths, frequently changed code. Refactoring — improve code structure without changing behavior. Pay down debt before adding features when velocity severely impacted. Prevent new debt: code reviews, architecture guidelines, automated quality gates. Communicate debt impact to stakeholders in business terms."),
        ("What is the twelve-factor app methodology?", "Twelve-factor app defines principles for building modern scalable software-as-a-service applications. Codebase — one codebase per app in version control, multiple deploys. Dependencies — explicitly declare and isolate all dependencies (requirements.txt, package.json). Config — store configuration in environment variables, not code. Backing Services — treat databases, caches, queues as attached resources replaceable without code changes. Build, Release, Run — strictly separate build (compile), release (config + build), and run stages. Processes — execute app as stateless processes, persist state in backing services. Port Binding — export services via port binding, app is self-contained web server. Concurrency — scale out via process model. Disposability — fast startup, graceful shutdown, handle crashes well. Dev/Prod Parity — keep development and production as similar as possible. Logs — treat as event streams, don't manage log files. Admin Processes — run admin tasks as one-off processes."),
    ],
}

@seed_bp.route("/seed")
def seed_questions():
    count = 0
    for role, questions in QUESTIONS.items():
        existing_count = Question.query.filter_by(role=role).count()
        if existing_count > 0:
            continue
        for q_text, a_text in questions:
            q = Question(role=role, category=role, question=q_text, answer=a_text)
            db.session.add(q)
            count += 1

    db.session.commit()
    total = Question.query.count()
    return {"status": "success", "newly_seeded": count, "total": total}
